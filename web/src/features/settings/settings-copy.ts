/**
 * 设置页与管理页的三段提示文案。
 *
 * ⚠️ **这三段是契约，不是文案建议**（SPEC §9.9、settings-ux.md §4）：不能删、不能弱化、
 *    不能改措辞。它们从规则原文逐字抄来，单独放在这个文件里是为了「改没改过」一眼可比——
 *    校验方式是把下面的字符串和 web/agents/rules/settings-ux.md §4 的代码块 diff。
 *
 * 「你的打标结果会进入公共库」一句尤其不能删——它是共享库代价在界面上的唯一体现。
 *
 * 渲染用 <pre>：文案的缩进和分行本身是结构（接口要求 / 警告 / 推荐 三段），
 * 拆成 <p> 重排会丢掉它，也会在下一次改动里悄悄走样。
 */

/** 用户设置页 · 视觉模型（settings-ux.md §4）。 */
export const VISION_NOTICE = `接口要求
  · 需兼容 OpenAI /v1/chat/completions
  · 必须支持图片输入（多模态）。纯文本模型会报 unsupported content type
  · 必须能稳定输出 JSON。优先选支持 response_format: {"type":"json_object"}
    或 tool calling 的模型
  · 不需要关心 GIF / WebP 支持 —— 系统会统一转成 PNG 再发送

⚠️ 你的打标结果会进入公共库，所有人都会搜到
  · 不填就用部署方的默认配置，那是经过验证的，多数情况下不需要改
  · 换成质量更差的模型，影响的是所有人的搜索结果

推荐
  · DeepSeek V4.1 Flash —— 中文语感好，单张约 $0.0007
  · 副通道建议配一个内容策略更宽松的供应商，用于主通道拒绝时降级`

/** 管理页 · Embedding（settings-ux.md §4）。 */
export const EMBED_NOTICE = `⚠️ 这是全站唯一一份 Embedding 配置，对所有用户生效

接口要求
  · 需兼容 OpenAI /v1/embeddings
  · 输出维度必须 ≥ 1024，系统会统一截断并重新归一化到 1024 维
  · 强烈建议选择支持 MRL（套娃表征）的模型，否则截断会明显降低检索质量
  · ⚠️ 更换模型会触发全站重新索引，期间搜索降级为 OCR + 标签

推荐
  · Qwen/Qwen3-Embedding-4B（硅基流动）—— 支持 MRL，中文强，延迟适中`

/**
 * 管理页 · 运行参数（字段与端点见 SPEC §5.6、§6.5.5，每条约束的由来见 §9.26）。
 *
 * 出处是 [SPEC §9.9] 的第三块「管理页 · 运行参数区域的提示文案」——2026-09-23 起才落进
 * SPEC，此前它只活在跨端任务书里（见任务 2026-09-23-runtime-config 的
 * 「总管裁定」裁定 1）。所以本文件头的 diff 目标 `settings-ux.md §4` 同批补上了这一段，
 * 三处逐字相同是校验得不出来的那种前提——**少一处就 diff 不出来**。
 *
 * 三句承重的话不能删，逐句理由在 §9.9：
 * ①「这几个数改的是全站资源分配，不是你的偏好」——它是这几个数归 admin、而不是每个
 *   用户自己配的理由在界面上的落点（SPEC §3.3）；
 * ②「保存后**新开的**任务按新值跑」——生效不是事务性的「立即」：打标等下一轮 tick、
 *   导入按批次读（SPEC §6.5.5）。不写「新开的」，用户会按「点完就该变快」去读，
 *   然后在第二个还在跑的任务上看出「没生效」；
 * ③「跑多个服务进程时，实际并发是这里的数 × 进程数」——这里每个数都是**每进程**的
 *   （SPEC §9.26）。删了它，① 就会被读成「全站上限 N 张」，而多副本下那句话是假的。
 *
 * ② 里的 `**新开的**` 是全段唯一的 markdown 强调：星号逐字留在字符串里（便于与 §9.9
 * 逐字 diff），加粗由渲染层做、渲染时剥掉星号。渲染处的处理见 RuntimeSettings.tsx。
 */
export const RUNTIME_NOTICE = `⚠️ 这几个数改的是全站资源分配，不是你的偏好

打标
  · 并发任务数 —— 同时打标几张。调大等于同时烧几份视觉调用的钱
  · 每用户在途 —— 一个人最多占几个槽；填得比「并发任务数」大是无效配置，会被拒绝

导入
  · 管线并发 —— 同时处理几个文件。超过下面的 ffmpeg 上限不会更快，只会排队
  · ffmpeg 上限 —— 同时几个 ffmpeg 子进程。每个可能吃数百 MB 内存，
    所以上限锁在本机核数

生效
  · 保存后**新开的**任务按新值跑。正在打的图和正在跑的导入批次不中途改
  · 有没有生效看这里：打标队列的积压下降速度（导入页 · 打标页签）
  · 跑多个服务进程时，实际并发是这里的数 × 进程数`
