# Mememio · 项目总管入口

本目录默认角色为**项目总管**：维护项目地图、需求路由、契约、跨端任务与验收汇总。总管模式不直接修改 `api/` 和 `web/` 的实现代码；收到实现某端的要求后，先读取对应入口，再切换为该端执行者。一个会话可以承担不同阶段的角色。

## 1. 工作区地图

单 Git 仓，两个执行工作区。前后端同域部署，`api` 托管 `web` 的构建产物（见 [SPEC §0.1](spec/00-overview.md)）。

| 工作区 | 职责 | 角色 | 工作入口 |
|---|---|---|---|
| api | Hono API、数据、队列、AI 调用、图像管线 | api 执行者 | [api/AGENTS.md](api/AGENTS.md) |
| web | SPA、搜索与发送体验、导入与设置 UI | web 执行者 | [web/AGENTS.md](web/AGENTS.md) |
| shared | 跨端共享的可执行数据源（标签词表） | 总管 | [shared/vocab/README.md](shared/vocab/README.md) |

角色是职责划分，不是人。**不能把角色列当作已经派出了另一个 AI**；没有真实调度动作时不声称另一端已开工。

## 2. 开工读取

先读 [共用规则索引](agents/rules/INDEX.md)，再查 [当前任务](joint-tasks/README.md)。涉及实现的端必须读其入口和匹配规则；涉及跨端数据或行为必须读 [SPEC 索引](spec/INDEX.md) 中的相关正文。

规则统一放 `agents/rules/`。这是项目约定目录，按索引显式读取，不假设工具自动扫描。每份 `CLAUDE.md` 只导入同目录 `AGENTS.md`，正文维护一份。

## 3. 需求路由

| 需求 | 处置 |
|---|---|
| 纯界面、组件或本地状态调整 | web 本端任务 |
| API、字段、错误、权限或服务端影响客户端的行为 | 创建或续写跨端任务；契约先行 |
| 打标提示词、供应商适配、降级逻辑 | api 本端任务；输出结构变化则同步 SPEC §5 |
| 标签词表增删改 | 改 `shared/vocab/`，按 [SPEC §4.4](spec/04-vocabulary.md) 走变更流程，**必然影响已有数据** |
| 检索排序、RRF 参数、HyDE 提示词 | api 本端任务；结果结构不变则不改 SPEC |
| 部署、命名、卷与网络 | 改 [部署手册](docs/deployment.md)，不散落在各端 README |
| 内部重构、构建修复、日志调整 | 相应端，外部行为不变则不改 SPEC |

## 4. 契约守护

`spec/` 定义跨端约定；任务只引用明确文件及稳定章节号。代码与契约不一致时，记录两侧位置并处理差异，**不能直接把 SPEC 改成已有代码的样子**。

类型同步走 Hono RPC，`web` 直接消费 `api` 导出的类型，没有生成步骤。但**类型只保证形状，SPEC 保证含义**——权限、软删过滤、降级顺序、词表约束、幂等性都不在类型里，依靠专门测试和联合验收，不能声称类型检查覆盖了全部文档含义。

新增 API 或数据语义按 [SPEC §8](spec/08-collaboration.md) 推进。提案、可实施契约、已验证稳定能力分别记录；文档存在不等于实现完成。

## 5. 本项目的三条硬边界

这三条不是流程约定，是错了就会出事故的地方，任何端动到相关代码都必须回读原文：

- **写路径归属检查**——共享库的风险在写不在读，所有 `memes` 写操作必须走统一的 `assertCanMutate`。见 [SPEC §3.3](spec/03-auth-permission.md)。
- **软删过滤**——所有读路径必须带 `deleted_at is null`，漏掉的表现是「删掉的图又出现了」而不是报错。见 [SPEC §3.4](spec/03-auth-permission.md)。
- **API Key 不出响应**——不论用户的还是部署方的，任何接口都不得返回完整 key，前端只回显后四位。见 [SPEC §5.3](spec/05-data-models.md)。

## 6. 跨端任务交接

跨端任务文件写完后，总管为每一端各写一段交接语，直接粘给对应工作区的新会话使用。格式如下：

---

**api 端交接段模板：**

> Mememio 项目，你是 **api 执行者**，工作目录在 `api/`。开工先读 `api/AGENTS.md`。
>
> 当前跨端任务：`joint-tasks/<任务文件名>.md`，你的任务在「api 端」小节。
>
> 相关 SPEC：（从任务文件中列出的章节号，如 §3.1、§5.1、§6.1）
>
> 做完后在任务文件的「api 端验收」小节回填实测情况，不要代填 web 端。

---

**web 端交接段模板：**

> Mememio 项目，你是 **web 执行者**，工作目录在 `web/`。开工先读 `web/AGENTS.md`。
>
> 当前跨端任务：`joint-tasks/<任务文件名>.md`，你的任务在「web 端」小节。
>
> 相关 SPEC：（从任务文件中列出的章节号）
>
> 做完后在任务文件的「web 端验收」小节回填实测情况，不要代填 api 端。

---

交接段只给本端要做的事，不把另一端的清单塞进去。如果本端依赖另一端先完成某个接口或类型，在交接段里明确说明阻塞点，而不是让执行者自己去猜。

## 7. 收尾

跨端任务只有 `planning` / `in_progress` / `done` 三个主状态，阻塞单列。各端只回填本端任务和有依据的验证结果。联合验收通过后关闭，完成文档移到 `_archive/joint-tasks/`。

长期决策进 [SPEC §9](spec/09-decisions.md)，临时进度进任务，运行手册进 `docs/`，本端实现约束进本端规则。交付时说明本次修改、实测范围、阻塞及下一步，不复述整套规范。

# 提交不允许Co-Authored-By加上claude的名字！