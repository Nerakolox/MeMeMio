# 模型配置与测试连接：用户视觉通道、全站 Embedding、重建索引

**状态：`done`** · 创建 2026-09-16 · 联合验收通过 2026-09-18（第四轮）

验收依据：`api` 26 文件 261 passed + `tsc` 0；`web` typecheck 0 + build 通过；web 收尾轮 81 条断言真 api + 真 Postgres + 真 Chromium 跑过 80 条。四轮验收，前三轮不通过的理由和裁定都在文末，**不要只读这一行**。

相关 SPEC：[§3.5](../spec/03-auth-permission.md)、[§5.3](../spec/05-data-models.md)、[§6.3.1](../spec/06-endpoints.md)、[§6.5](../spec/06-endpoints.md)（含本任务新加的 §6.5.3 / §6.5.4）、[§9.3](../spec/09-decisions.md)、[§9.6](../spec/09-decisions.md)、[§9.7](../spec/09-decisions.md)、[§9.9](../spec/09-decisions.md)

## 为什么

**「模型可配置」这件事现在只存在于表结构里。** `user_ai_configs` 和 `embed_config` 两张表在 `api/src/data/schema.ts` 里建好了，`CONFIG_ENC_KEY` 在 `lib/env.ts` 里校验了长度，`lib/redact.ts` 的 `maskApiKey` / `isMaskedApiKey` 也写好了——但**没有任何代码读写这两张表**，也没有 AES-GCM 的加解密实现。跑起来的系统只认部署方那三个环境变量。

这不只是缺一个设置页，它在拖着三处已经记在板上的偏差：

| 现状 | 后果 |
|---|---|
| `resolveVisionConfig()` 固定 `jsonModeWorks: null, multiImage: null` | 降帧梯子 `[10, 4, 1]` 在生产里走不到第一级——`multiImage` 永远是 null 就永远按最保守的拼图模式走（[打标队列遗留项](README.md#打标队列任务转出的遗留项)） |
| `ai/hyde.ts` 直接 `asCredentials(env.defaultVision)` | HyDE 用部署方的通道，不是搜索者本人的——**与 SPEC §6.3.1 / `ai-providers.md §6` 的已知偏差**（[搜索遗留项](README.md#搜索任务转出的遗留项)） |
| `resolveEmbedConfig()` 只读环境变量 | 部署方没配 `DEFAULT_EMBED_*` 时向量路永久缺失，管理员没有任何办法把它配上 |

**而「测试连接」是 §6.5 里最有价值的那个接口**，不是附属功能。用户填的是中转服务，[§9.7](../spec/09-decisions.md) 的整个论点就是「不让用户手填能力、也不假设任何能力，全部实测」；能力位（`jsonModeWorks` / `multiImage` / `dimParamWorks`）是运行时分支的**唯一**依据，而它们只能由这个接口写入。没有它，`ai-providers.md §2` 那张表里的三个字段全是 null，整个「能力从配置读」的设计落不了地。

顺序上它也该在 §6.4（编辑 / 软删 / retag）之前：`POST /memes/retag` 要用「调用者自己配置的视觉模型」，那个前提得先存在。

## 做完的标准

1. 一个新注册的普通用户能在 `/settings` 里填自己的视觉通道、点测试连接看到逐项探测结果、保存后导入的图**用他自己的 key 打标**（日志里能看出走的是哪套配置）
2. 测试不通过点不了保存；服务端也拒（`CONFIG_TEST_REQUIRED`）。改了任一字段后之前的测试结果不作数
3. 测试失败时 `rawResponse` / `rawError` **原样**出现在界面上（等宽、可折叠、可复制），没有被截断或包装成友好文案
4. 探测到的能力位真的改变了运行时行为：`multiImage: true` 的配置打一张 12 帧动图，日志显示发了 10 张 PNG 而不是拼图
5. 任何响应、任何日志、任何错误 `details` 里都搜不到完整 key，也搜不到 `CONFIG_ENC_KEY`。数据库里 `*_api_key_enc` 是密文，直接 `select` 出来读不出原文
6. admin 能在管理页配全站 Embedding：维度 < 1024 被拒（`EMBED_DIM_TOO_SMALL`）；库里有数据时换模型不带 `confirmReindex` 被拒（`EMBED_MODEL_CHANGED`）
7. 确认换模型后全站重建索引自动开跑，进度可查；**期间搜索照常返回结果且带 `degraded: true`**，不中断
8. 非 admin 访问 `/config/embed`、`/admin/reindex*` 一律 403（`FORBIDDEN`），前端也不给入口
9. `settings-ux.md §4` 那两段文案**逐字**出现在界面上，「你的打标结果会进入公共库」一句在
10. 闸门：`cd web && npm run typecheck` 0 错误；`cd api && npm test` 全绿

## 必须先知道的四件事

这四条是本任务最容易做错的地方，两端各自相关的部分在下面的小节里还会出现，但先在这里讲清楚理由。

### 1. 测试记录存在服务端，客户端不经手探测结果

探测结果字段**不接受客户端写入**（[§5.3](../spec/05-data-models.md)），所以「我刚测过了」这件事不能靠前端在 `PUT` 里回传。流程被这条硬规则固定成：

```
POST /config/vision/test  →  服务端实测 → 结果存服务端（含被测的 baseUrl / model / key 指纹）
PUT  /config/vision       →  按 baseUrl + model + key 指纹找到那条成功记录 → 把探测结果抄进配置行
```

匹配要带 **key 指纹**（key 的 SHA-256），不能只按 baseUrl + model——否则「换了 key 没测就保存」能过校验，而那恰恰是最常见的填错方式。这一条是本任务对 §6.5.2 的补严，已经写进 SPEC。

测试记录里**不存 key 明文也不存密文**，只存指纹。存法（新表还是别的）属 api 实现细节，SPEC 不管。

### 2. 运行时配置解析只有三个落点，不要扩散

```
api/src/ai/vision.ts    resolveVisionConfig()    ← 代码里已经标了「配置任务上线后只改这一处」
api/src/ai/embedder.ts  resolveEmbedConfig()
api/src/ai/provider.ts  asCredentials()          ← 「DB 里的记录排在环境变量前面，这个函数是那个改动的唯一落点」
```

`ai/hyde.ts:57` 现在绕过了前两者直接读 `env.defaultVision`，本任务要把它接回视觉通道的解析路径——**HyDE 用搜索者本人的配置**（`ai-providers.md §6`）。

这三处之外不许再出现「先查库、查不到用 env」的分支。多一处就多一个会忘记加解密、忘记查 `verified_at` 的地方。

### 3. 送去实测的那张图不是随便一张

视觉测试要「发一张内置测试图 + **正式的打标提示词**」（[§9.7](../spec/09-decisions.md)），响应里的 `vocabCompliant` 是「返回的标签落在词表内」。这意味着：

- 测试图必须是**一张真的能被打出标签的图**。拿噪声图或纯色块去测，一个好模型也会返回空字段，那正好长得像 `ai-providers.md §3` 的第三种拒绝形态——测试连接会把能用的供应商判成不能用
- 测试图必须随 api 运行时可用。**不要引用 `docs/fixtures/`**——那批是测试与评测用的，`huge.png` 还被 `.gitignore` 排除着（[docs/fixtures.md](../docs/fixtures.md)），运行时依赖它等于依赖一个不保证存在的目录
- 选了哪张、为什么选它，在验收里写一句

### 4. 重算不许重跑视觉

重建索引是从 `search_text` 重新算向量、写 `embedding` + `embed_model`（`data/memes.ts` 已有 `updateMemeEmbedding`）。**不碰 AI 产出字段、不调视觉模型**——换 embedding 模型不影响打标结果，重跑视觉是白花钱，而且会把用户手工编辑过的字段覆盖掉。

## 两端各自做什么

### api 端

**A. 加解密**

- AES-GCM 的 encrypt / decrypt，主密钥 `env.configEncKey`。放 `lib/`，纯函数、可单测
- 解密出来的 key **只在调用那一瞬间存在**：不进长生命周期对象、不进日志、不进错误 `details`、不进响应（`ai-providers.md §7`、`error-handling.md`）
- `CONFIG_ENC_KEY` 本身不进任何接口、任何日志、任何 `details`（[§3.5](../spec/03-auth-permission.md)）
- 单测至少覆盖：往返一致、密文每次不同（nonce 不复用）、换密钥后解密失败且失败方式是可判断的错误而不是脏数据

**B. 数据访问层**

- `data/` 里新增 `user_ai_configs` 与 `embed_config` 的读写。SQL 只在 `data/`（`project-structure.md`）
- `embed_config` 是单行表（`check (id = 1)`），写入用 upsert，不要 `insert` 后自己判重
- 探测结果字段只由测试路径写入。**读写函数的签名就该让「客户端传来的对象」没法写到这些字段上**，靠注释提醒挡不住下一个人

**C. 六个端点**（[§6.5](../spec/06-endpoints.md)、§6.5.3、§6.5.4）

| 端点 | 要点 |
|---|---|
| `GET /config/vision` | 返回 §6.5.3 那个形状；未配置时回显部署方默认值并带 `source: "default"` |
| `PUT /config/vision` | 脱敏串 = 不修改（用 `isMaskedApiKey`，别再写一遍）；无匹配测试记录 → `CONFIG_TEST_REQUIRED` |
| `POST /config/vision/test` | **不通过也是 200**，结果在响应体里（`http.md`、`settings-ux.md §5`）。只有请求本身非法才是 4xx |
| `GET / PUT /config/embed` | admin only；`nativeDim < 1024` → `EMBED_DIM_TOO_SMALL`；换模型且库里有数据缺 `confirmReindex` → `EMBED_MODEL_CHANGED`（409） |
| `POST /config/embed/test` | 同上，实测维度、`dimensions` 参数是否生效 |
| `POST /admin/reindex` · `GET /admin/reindex/status` | 幂等；进度来自库里的真实计数，不是内存计数器 |

权限：`/config/*` 是本人，`/config/embed*` 与 `/admin/*` 是 admin。用已有的中间件，不在 handler 里手写角色判断（`api/AGENTS.md §3`）。

**D. 探测实现**

- 视觉：能否收图 / 是否合法 JSON / 标签是否在词表内 / `response_format: json_object` 是否生效 / 多图是否支持。判定复用 `lib/vision-output.ts` 和 `lib/vocab-match.ts`，**不要为测试连接另写一套判定**——两套判定迟早给出不同结论
- Embedding：实测维度、`dimensions` 参数是否生效（传了以后返回的维度真的变了才算生效），`willTruncate` 由实测维度推出
- 不许出现按 `baseUrl` 猜供应商的分支（`ai-providers.md §1`）
- `rawResponse` / `rawError` 原样带回。**带回之前确认它不可能包含 key**——原始返回里一般没有，但如果实现把请求头或整个 request 一起塞进了 raw 字段，这个「核心价值」就变成一条泄露路径。这是本任务唯一一处「原样返回外部数据」和「key 不出响应」正面相撞的地方，处理方式在验收里写出来

**E. 运行时接入**

- 三个落点改成「DB 优先、环境变量兜底」，`hyde.ts` 接回视觉解析
- 解析结果只认**通过过测试**的配置行；测过但没通过的行不能进生产打标
- ⚠️ `queue/worker.ts:139` 的 `isVisionConfigured()` 是个**全局**判断：部署方没配默认视觉通道时 worker 60 秒才轮询一次且不取任务。用户自带 key 之后这个前提不成立了——照现在的写法，「部署方没配、用户自己配了」的图会永远 pending。改法自定，但这条必须处理，并在验收里说明怎么测的

**F. 重建索引**

- 用同一套队列机制，**不要塞进 `tag_jobs`**：那张表上 `meme_id` 有唯一索引，一条重算任务会和同一张图的待打标任务互相踢掉；而且重算不调视觉、重试策略也不同
- `PUT /config/embed` 带 `confirmReindex: true` 时自动入队；`POST /admin/reindex` 是手动补触发，两者都要幂等（`onConflictDoNothing` 的路子）
- 并发按全站一个小上限控，**不按用户分组**——这是运维操作，不是谁的配额
- 搜索侧：有未完成的重算任务时 `degraded: true`。判断落在每个搜索请求上，要么是索引命中的存在性查询，要么带短期缓存，**不能是全表 count**
- 不做断点续传（`queue.md §7`）

**G. 迁移**

新增列或新表都要出迁移文件，按 `api/migrations/` 现有编号续写。

### web 端

**A. `/settings`（所有人）**

- 视觉通道那一组收进**默认折叠**的「高级选项」。这个折叠是有意的：改错了会污染所有人的搜索结果（`settings-ux.md §3`）
- 显示当前用的是部署方默认还是自己的配置（`source` 字段）
- `settings-ux.md §4` 的「用户设置页 · 视觉模型」整段文案**逐字照抄**，它是契约（[§9.9](../spec/09-decisions.md)）。「你的打标结果会进入公共库」这一句尤其不能删
- Key 输入框 password 类型，显示 `**** + 后四位`，**不加显示明文的眼睛图标**；不改 key 就把脱敏串原样提交

**B. 管理页 · Embedding（仅 admin）**

- 放哪个路由 web 自己定，但必须走已有的 admin 守卫，和 `/admin/invites`、`/admin/users` 一致
- `settings-ux.md §4` 的「管理页 · Embedding」整段文案照抄
- 换模型的确认框要说清：**会触发全站重算，期间搜索降级为 OCR + 标签，但不中断服务**
- 重建进度显示在管理页，**不做成模态阻塞**——它要跑几分钟，管理员应该能去干别的

**C. 测试连接控件**（`settings-ux.md §5`，这一页最重要的控件）

- 每组配置一个独立的测试按钮，结果**逐项**列出探测到了什么，不是一个成功／失败的红绿灯
- 失败时 `rawResponse` / `rawError` 原样展示：等宽、可折叠、可复制，**不截断、不包装**
- 测试连接返回 200 也可能是不通过，**不要当 HTTP 错误处理**——那会把 `rawResponse` 丢掉（`http.md`）
- 测试不通过禁用保存按钮；改了 baseUrl / model / key 任一字段就重置测试状态

**D. 样式**

只写布局（`web/AGENTS.md §5`）：flex / grid、尺寸、间距、折叠区的 `display`。不写颜色、字体、阴影、圆角。「标红提示」这类要求用结构和文案表达，不靠颜色。

**E. 类型**

接口类型从 api 的 Hono RPC 派生，不手写（`web/AGENTS.md §4`）。`source` 的两个取值、能力位可能是 `null`（未测过）——**类型只保证形状**，null 的呈现要自己处理，别让界面显示「不支持多图」而实际是「还没测」。

## 明确不做

| 不做 | 为什么 |
|---|---|
| 视觉**副通道**的表单与降级接入 | 字段留在表里不动。[§9.5](../spec/09-decisions.md) 还是 `proposed`，副通道接入是它的事（板上已有这条遗留项）。⚠️ 照抄的文案里有一句「副通道建议配一个内容策略更宽松的供应商」——界面上没有对应输入框。**不要自行删这句文案**，觉得会误导就在本文件里提出来，由总管裁定 |
| 设置页与管理页的**统计面板**（`settings-ux.md §9`） | 它需要 §6.5 之外的新端点，和本任务的契约无关。单独开任务 |
| §6.4 的编辑 / 软删 / restore / retag / 查重 | 另一个任务。`retag` 依赖本任务，顺序在后 |
| `queue.md §6` 的五个定时清理任务 | 与配置无关 |

## api 端验收

回填时间 2026-09-17 · api 执行者

### 实测环境（先说清楚这次测的是什么）

- **闸门两条都过**：`cd api && npm test` → 26 个文件 **259 passed**（本任务新增 5 个测试文件 **59 条**：`src/lib/config-crypto.test.ts` 13、`src/ai/probe.test.ts` 12、`tests/config-endpoints.test.ts` 20、`tests/config-user-vision.test.ts` 6、`tests/reindex-queue.test.ts` 8 —— 前两个是单测，后三个连真 Postgres）；`cd web && npm run typecheck` → **0 错误**。`npx tsc -p tsconfig.json --noEmit` 在 api 侧也是 0 输出。
- **六个端点已经挂进 `app.ts`**，并且是接在原来那条链上的（`.use().route()…`，`app.ts:21-30`），没有断链。web 端的阻塞点（E 项）现在可以解了。
- **没有调用过任何真实供应商。** 所有探测与打标路径都是 `vi.stubGlobal('fetch', …)` 的替身，替身按 OpenAI 兼容形状回，包括 401 / 502 / 维度不匹配 / 忽略 `dimensions` 参数这些失败形态。能这样测是因为本端不许按供应商分叉（`ai-providers.md §1`），代码里只有一条 HTTP 路径；**但这意味着「某个真实中转的怪癖」不在本次实测范围内**，只有接上真 key 才知道。
- 完成标准 4（12 帧动图实测发 10 张 PNG）走的是已有的 `tests/tag-queue-degrade.test.ts` / `tests/tag-queue.test.ts`，本次把「没有探测记录时不发多图」那条对照也保住了，两个文件仍全绿。

### A. 加解密（`src/lib/config-crypto.ts`，13 条单测）

AES-256-GCM，`版本号 || nonce(12) || ciphertext || tag(16)` 拼成一段 `bytea`。9 条打加解密：往返一致（含中文、含空串——key 是用户填的，不假设字符集）、**同一明文加密两次密文必然不同**（nonce 不复用）、密文里搜不到明文、换主密钥是**可判断的错误**而不是脏数据、密文被改一个字节也解不开（GCM 校验的是完整性，防的是「有人直接 UPDATE 了那一列」）、不是本函数写出来的数据分成 `malformed` / `unsupported_version` 两种可判断的失败、主密钥长度不对**直接拒绝而不静默补齐**、**错误 message 里既不含明文也不含主密钥**（SPEC §3.5 要求两者都不进 `details`）。另 4 条打 key 指纹：同 key 同指纹、定长十六进制且不含原 key、**一条已知向量**（换实现也得算出同一个值，否则库里老的测试记录会全部失配，表现是所有人突然被要求重测）、`fingerprintEquals` 只对相同指纹为真。

`CONFIG_ENC_KEY` 的流向是收口的：`lib/env.ts` 读一次，**`data/ai-configs.ts` 是唯一把它传进加解密的地方**（文件头写明了这一点），`logger.ts` 把它列进脱敏字段名单。`routes/` 和 `services/` 里一次都没出现过。

### B. 数据访问层（`src/data/ai-configs.ts`、`src/data/reindex-jobs.ts`）

`embed_config` 用 `onConflictDoUpdate` 的 upsert，没有「先查再判」。探测结果字段挡在**签名**上而不是注释上：写配置的两个函数只收 `ProviderInput`（`{ baseUrl, model, apiKey }`），能力位根本不在参数里，由函数自己从 `config_tests` 里那条匹配记录抄过去。这条在 `tests/config-endpoints.test.ts` 里正面打了一枪——`PUT /config/vision` 的请求体里塞 `jsonModeWorks: false, multiImageWorks: false`，响应回来仍然是真实探测的 `true / true`。

### C. 六个端点（20 条集成测试）

逐条对应完成标准的实测结果：

| 标准 | 实测 |
|---|---|
| 未配置时回显部署方默认值 | `source: "default"`、`apiKey: "****3344"`、能力位**全 null**（部署方那套也没有测试记录，不假设它支持什么） |
| 测试不通过是 200 | 替身回 401，响应 **200**，`ok: false`，`rawResponse` 在体里。只有请求体非法（缺 `baseUrl`、`baseUrl` 不是 http(s)）才 400 |
| 测试连接不改当前配置 | 测完再 `GET`，`source` 仍是 `default` |
| `CONFIG_TEST_REQUIRED` | 没有测试记录直接 `PUT` → 400；**baseUrl / model 都没变、只换了 key** 也照样 400（匹配带 key 指纹） |
| 脱敏串 = 不修改 | 回传 `****7788` + 改 model → 200，尾号不变；此前没配过 key 却传脱敏串 → 400（不静默存空 key） |
| `EMBED_DIM_TOO_SMALL` | 实测 512 维 → 400，`details: { nativeDim: 512, required: 1024 }` |
| `EMBED_MODEL_CHANGED` | 库里有向量时换模型 → **409**，`details: { currentModel, nextModel }`；带 `confirmReindex: true` 重发 → 200，`reindexEnqueued: 1`，队列里确实多了一条 pending。库里没有任何向量时**不拦**（没有东西要重算）；第一次配置不算换模型 |
| 403 | member 打 `GET/PUT /config/embed`、`POST /config/embed/test`、`POST /admin/reindex`、`GET /admin/reindex/status` 五个全 403 |
| 进度来自库 | 入队前 `{ running: false, total: 1, done: 0, stale: 1, failed: 0 }`，入队后 `running: true` 而 **`stale` 不变**——两个数说的是两件事，都对 |
| 重建幂等 | 连按两次 `POST /admin/reindex`：`enqueued` 1 → 0，队列里仍是一条 |

**「key 不出响应」不是单独一条用例**，是每条成功路径上的公共断言：`readJsonNoKeys()` 把响应体当字符串搜四把明文 key（部署方视觉/embedding、用户视觉/embedding），任何一把出现就红。这么写是因为只在一处验的话，下一个人给响应加个字段就漏了。另外直接 `select` 了 `user_ai_configs.vision_api_key_enc`，把 bytea 当 utf8 读也搜不到原文。

### D. 探测实现 —— 「`rawResponse` 不含 key」是怎么测的

这是本任务唯一一处「原样返回外部数据」和「key 不出响应」正面相撞的地方，所以没有用「我检查过出口只有一个」这种说法交差——那句话下一个人改一行就不成立了。做法是**复现真实的泄露形态再打**（`src/ai/probe.test.ts` 头四条）：

`error-handling.md §4` 明写某些中转服务会把 `Authorization` 头回显进错误体，而那恰好是**填错 key 拿 401** 时最容易发生的事，也正是测试连接最常触发的路径。于是用例让替身回一个真实形状的 401 体，里面 key 出现**两次**（一次在 `message` 的 "Incorrect API key provided: sk-…"，一次在 `error.request.headers.authorization`），然后五条断言连起来：

1. `requests[0].authorization === 'Bearer sk-proj-…7788'` —— **先证明明文真的发出去过**。少了这条，后面四条可能只是因为这次压根没发请求；
2. 泄露体里 `split(KEY).length - 1 === 2` —— 确认这一轮确实有两处可泄露；
3. `report.rawResponse` 里搜不到 KEY —— 一个字节都不剩；
4. `rawResponse` 里 `****7788` 出现**两次** —— 是**换成**脱敏串而不是删掉，用户得看得出「这里本来是你的 key」；
5. `report.rawResponse === leaked.split(KEY).join(MASKED)` —— **字节级全等**。这条同时钉死了另一半要求：除了 key 之外一个字节都没动，没截断、没包装成友好文案（`settings-ux.md §5`、完成标准 3）。

另外三条补住了出口的完整性：embedding 侧走的是 `rawError` 这条**另一个**出口，同样被替换；响应体里**没有** key 时原文一个字节不动（502 的 HTML 原样返回——抹 key 不能顺手删掉用户真正需要的那行错误）；**成功路径**的 `rawResponse` 也过同一个 `finish()`，验的是「没有第二条出口」。

脱敏用的是已有的 `lib/redact.ts` 的 `maskApiKey`，没有在探测里另写一遍。实现上 raw 字段只装**响应体**，请求头和请求对象从来没进过它们。

其余 8 条探测单测：第一次就打不通时**只发 1 次请求**（不拿用户的钱确认同一件事），能力位全 null；收到图但输出不合格时 `canReceiveImage: true` 而 `vocabCompliant` 是 **null 不是 false**（「拿不到可校验字段」和「字段不在词表里」是两件事，报 false 会让用户跑去改提示词）；三次都成功时断言基线那次**不开** `response_format`、另一次开了、多图那次 content 里真的有 **2** 个 `image_url`，且三次 URL 是同一个端点（没有按供应商分叉的第二条路径）；标签越界时 `vocabCompliant: false` 但仍 `ok: true`，判定复用 `src/vocab.ts` 那份适配器——另写一套的话，「测试连接说能用、打标时标签全被丢掉」不会被任何测试抓住。Embedding 侧钉的是「中转把 `dimensions` 忽略掉」这一路：只看状态码会把它判成生效，然后运行时拿一个没归一化的长向量去写库，不报错、只是搜索悄悄变差。

### 送去实测的那张图：选了哪张、为什么

`src/ai/probe-image.ts` 里一个 base64 常量，**PNG，384×384，9675 字节**，sha256 `c9d4c5c6744e61633558949f084e6a29521cb0a7ea8e2434878f19c3827a1032`（验收时把常量解码回 PNG 重算过，和 `PROBE_IMAGE_SHA256` 一致，并**打开看过渲染结果**：卡通猫、半睁的死鱼眼、黑眼圈、平嘴、桌沿线，字幕「今天不想上班」六个字完整成形，没有豆腐块）。生成脚本留在 `scripts/gen-probe-image.ts`。

为什么是这张：

- **它是一张真能被打出标签的图**。§9.7 要求发内置测试图 + **正式的打标提示词**，`vocabCompliant` 判的是返回标签落在词表内。噪声图或纯色块会让一个好模型也返回空字段，那正好长得像 `ai-providers.md §3` 的第三种拒绝形态——**测试连接会把能用的供应商判成不能用**。这张图是对着 `shared/vocab/vocab.json` 设计的，五个字段每个都有闭集里的词可落：主体（猫、文字）、情绪（疲惫 / 生无可恋 / 无奈）、场景（上班摸鱼 / 加班 / 周一）、风格（手绘 / 简笔画 / 带字幕），OCR 文本就是那六个字。
- **编进源码，不读文件系统**。不引用 `docs/fixtures/`——那批是测试与评测用的，`huge.png` 还被 `.gitignore` 排着（`docs/fixtures.md`），运行时依赖它等于依赖一个不保证存在的目录。另一个理由是部署：根 `Dockerfile` 的最终镜像只 COPY `node_modules` / `dist` / `migrations` / `public` 和 `/shared`，**新加一个 `api/assets/` 不会进镜像**，而改 Dockerfile 属于部署事项、归总管（`AGENTS.md §3`）。编进 `dist/` 的常量零部署改动，也不可能在运行时「文件不见了」。
- **纯矢量、无人脸、无版权素材**：探测图自己被判 `AI_REFUSED` 的话整个测试连接就废了，这条比画得好看重要。
- **384×384 / 9675 字节**是按「一次探测发三遍、其中一遍发两张」定的：全程约 40KB base64，探测一次的成本可以忽略，分辨率又够模型读清那六个字。
- 中文字幕是有意的：本项目的图基本都带中文，用英文图测等于测了一个和生产不一样的东西。

⚠️ 一个给下一个人的提醒：字幕是 SVG `<text>` + 系统字体（`Microsoft YaHei, SimHei, Noto Sans CJK SC`）渲染的，**重跑生成脚本的产物依赖本机装了哪套中文字体**，换台机器重跑很可能得到不同的 sha256，没有中文字体的机器上还会渲染成豆腐块。运行时不受影响（走的是已提交的 base64 常量）；但**改图必须重跑脚本、在图里确认字还在、再把新的 sha256 一起提交**，脚本文件头里写了这件事。

### E. 运行时接入 —— `isVisionConfigured()` 是怎么测的

三个落点都改成了「DB 优先、环境变量兜底」，`hyde.ts` 接回了 `resolveVisionConfig(actorId)`（匿名搜索没有「本人」，那一支仍落到部署方默认值）。解析只认 `verified_at` 非空的配置行。

`queue/worker.ts` 那个全局判断改成了「部署方配了 **或** 库里有任何一条通过测试的用户配置」。**这条 bug 的表现是没有表现**——不报错、不告警、不重试，只是那些图永远停在 pending，所以断言不能只看「跑通了」。测法（`tests/config-user-vision.test.ts`，6 条）：

1. **把部署方那套彻底清空**——`DEFAULT_VISION_*` 与 `DEFAULT_EMBED_*` 六个变量在任何 `src` 导入**之前**置空（`env.ts` 首次 import 就把值冻住，同一个文件里没法既有配置又没配置，所以单开一个文件，同 `tag-queue-unconfigured.test.ts`）。这样「部署方兜底」这条路被物理切断，剩下的只可能来自库。
2. 三条直接打函数：两边都没有 → `false`；**有一条通过测试的用户配置 → `true`（这条就是改之前的 bug，改之前必红）**；`verified_at` 为 null 的行**不算数** → `false`，且 `resolveVisionConfig()` 也返回 null（「值不值得取任务」和「能不能进生产打标」必须是同一条判据，否则 worker 会为一条永远解析不出来的配置不停空取）。
3. **端到端跑真 worker**：起 `startTagWorker()`，塞一张真图进队列，轮询到 `tag_jobs.status === 'done'`、`last_error` 为 null、`memes.tag_status === 'ok'`。没修之前这里会**超时**而不是失败——worker 压根不取任务，这正是线上的症状。
4. **断言发出去的是哪一把 key**：抓所有出站请求的 `Authorization` 头，`expect(new Set(authHeaders)).toEqual(new Set(['Bearer sk-user-own-key-…']))`。只断言 `tagStatus: 'ok'` 是不够的——**拿别人的 key 跑通也一样是 ok**，这条才是「用他自己的 key 打标」的硬证据。同一条里还验了 `vision_model` 落的是用户填的那个模型名，以及 embedding 没配时 `embedding` 为 null 而打标照常完成（正常降级，不是失败）。
5. **没配的那个用户仍然 `not_configured`**，不受别人配置的影响——`isVisionConfigured()` 为真只说明「值得去取任务」，取到之后还要按上传者逐条解析。这条防的是把全局判断当成逐条判断用。

### F. 重建索引（8 条集成测试）

独立的 `reindex_jobs` 表和**独立的 worker**，没有塞进 `tag_jobs`（那张表 `meme_id` 上有唯一索引，一条重算任务会和同一张图的待打标任务互相踢掉）。并发全站上限 4，不按用户分组。两个 worker 在 `server.ts` 里都是无条件启动、停机时 `Promise.allSettled` **并行**收尾（串行等于把两个超时上限加起来，停机时间平白翻倍）。

- **「重算不许重跑视觉」是用出站请求验的，不是读代码**：跑完一轮重算后断言 `calledPaths.every(p => p.endsWith('/v1/embeddings'))`，一次 `chat/completions` 都没发过。同一条用例逐个断言 `description` / `tags` / `vision_model` / `search_text` **四个字段一个字没变**——「把用户手工编辑过的字段覆盖掉」才是这条规则真正防的事故。
- 排队期间图被软删 → `gone` 而不是 failed，且**一个请求都没发**；没配 embedding 通道 → `not_configured`，不消费重试次数。
- 入队：软删的不入队；已经是当前模型的不入队；没配 embedding 模型时整个操作是空操作（不排一堆必然失败的任务）。
- **完成即删行**：worker 跑空队列后 `reindex_jobs` 里 0 行。留着 done 记录的话，这张图下一次换模型时会被 `onConflictDoNothing` **静默**跳过。
- **搜索侧降级**：有未完成任务时 `degraded: true` **且结果一条不少**（两条同时断言——只验 degraded 的话，把搜索改成直接返回空也能过）；队列空时 `degraded: false`。判断用的是 `hasUnfinishedReindexJobs()`（`limit 1`，命中 claim 索引，不是全表 count），而且它是**和三路召回塞进同一个 `Promise.allSettled`** 的第四个元素，不串在前面也不串在后面——再便宜的查询串行也是给每个搜索请求加一个往返。这条查询自己失败时按「没在重算」处理并打 warn：为了一个展示用的字段让整次搜索 500 是不划算的。

`degraded` 没有拆成两个字段（向量路缺席 / 正在重算），理由写在 `services/search.ts` 的类型注释里：前端的动作是同一个（挂一条「结果可能不全」），拆开会让「两个都为真」时的文案变成一道组合题；具体是哪种原因写在日志里。

### G. 迁移

`migrations/0005_add_config_tests_and_reindex_jobs.sql`（`config_tests`、`reindex_jobs`，含队列的 claim 索引和 `meme_id` 唯一索引），编号接着现有的续写，`meta/_journal.json` 与 `0005_snapshot.json` 同步。`user_ai_configs` / `embed_config` 两张表和它们的 `bytea` 列、能力位列在 `0000` 里就有，本次没有动表结构。

`tests/helpers/test-db.ts` 的 `truncateAll` 补进了 `reindex_jobs`、`user_ai_configs`、`config_tests`、`embed_config` 四张表。**这条漏了不会报错**——只是上一个用例的残留会漏到下一个用例，表现成「单独跑绿、整批跑红」；`embed_config` 是全站单例表，残留一行就会让下一个用例以为 embedding 已经配好了。整批 259 条与单文件分别跑，结果一致。

### 本次改了什么

新增：`lib/config-crypto.ts`、`ai/probe.ts`、`ai/probe-image.ts`、`data/ai-configs.ts`、`data/reindex-jobs.ts`、`routes/config.ts`、`services/ai-config.ts`、`services/reindex.ts`、`queue/reindex-worker.ts`、`scripts/gen-probe-image.ts`、迁移 0005，以及上面列的 5 个测试文件。

改了：`ai/vision.ts`、`ai/embedder.ts`、`ai/provider.ts`、`ai/hyde.ts`（四个运行时解析落点）、`routes/admin.ts`（挂重建两个端点）、`app.ts`（链式挂载六个端点）、`queue/worker.ts`（`isVisionConfigured`）、`services/tagging.ts`、`services/search.ts`（degraded）、`server.ts`（第二个 worker 的起停）、`data/memes.ts`、`data/schema.ts`、`lib/redact.ts`、`src/vocab.ts`、`tests/helpers/test-db.ts`。

**没有动 SPEC。** 没有动打标提示词、词表和检索参数，所以**没有跑评测集**——本次改动不涉及会影响打标或召回质量的部分（探测用的是同一份正式提示词，但没有改它）。

### 还没做 / 已知限制

1. **没接过真实供应商**。全部是 fetch 替身。真 key 接上去之后，`rawResponse` 的实际内容、某些中转对 `response_format` / `dimensions` 的怪癖，才算真正验过。
2. **重建索引只在小数据量下跑过**（用例里 1~2 条）。`ENQUEUE_CAP = 100_000`、分页入队、并发 4 这几个数是按设计定的，没有在上万条的库上实测过吞吐和 HNSW 写入压力。
3. 完成标准 1 的「日志里能看出走的是哪套配置」目前是**间接**满足的：保存配置那几条日志打 `userId` / `baseUrl` / `model`（不打 key），打标失败那条打 `model`，靠模型名区分走的是谁的通道；**没有**一个显式的 `source` 字段进日志。真要一眼看出「这张图用的是用户配置还是部署方默认」，得在打标成功路径上补一条带 `source` 的日志——本次没做，也没在真实运行中人工核对过日志输出。
4. 视觉**副通道**按「明确不做」原样留在表里，没有任何读写。

> 2026-09-18 收尾轮（`reindexEnqueued` → `reindexEnqueuedCount`）追加在文末的
> 「[api 端验收 · 收尾轮](#api-端验收--收尾轮)」，本节保持上一轮原样。


## web 端验收

回填时间 2026-09-16 · web 执行者

### 实测环境（先说清楚这次测的是什么）

**六个端点还没挂上 `app.ts`**（`api/src/app.ts` 目前只 route 了 health / auth / admin / memes / search / imports，`routes/` 下没有 `config.ts`），所以这次**没有跟真接口联调过**。为了不把「本地看起来没问题」当成测过，做法是：

- 写了一个临时替身服务按 §6.5.3 / §6.5.4 的形状返回数据，通过 `vite.config.ts` 里已有的 `MEMEMIO_API_PROXY` 接进 dev server（那条 proxy 的注释里写明就是给联调替身用的）
- 用真 Chromium（Playwright）跑了一遍两个页面，**36 条断言全过**，桌面 1280×900 与手机 390×844 两个视口
- 替身和驱动脚本都是临时文件，验收跑完已删除，没有进仓库

替身能验的是**前端的行为**：折叠、文案、脱敏、保存闸门、三态渲染、原样展示、非模态、守卫。**验不了的是契约含义**——字段名对不对、`ok:false` 的真实形状、409 的真实 code，这些要等 api 挂上去联调，见下面的阻塞点。

闸门：`npm run typecheck` 0 错误，`npm run build` 通过（71 modules）。

### A. `/settings`（对应完成标准 1 / 2 / 3 / 9）

| 做完的标准 | 实测 |
|---|---|
| 视觉通道默认折叠 | `<details className="settings-section__advanced">` 不带 `open`，断言 `getAttribute('open') === null` 通过 |
| 显示当前用的是哪一套 | 折叠区**外面**显示 `source`，收起状态也能看见「部署方的默认配置」/「你自己的配置」；保存后实测从前者变成后者 |
| 文案逐字 | `settings-copy.ts` 里的两段是用脚本从 `agents/rules/settings-ux.md` §4 的代码块生成的；验收时在浏览器里把渲染出来的 `innerText` 和规则文件原文做了**字符串全等**比对，两段都过。`<pre>` 渲染以保住三段式结构 |
| 「你的打标结果会进入公共库」在 | 单独断言了这一句在 |
| Key 输入框 | `type="password"`，值就是脱敏串 `****1234`（不改动就原样提交）；**没有眼睛图标**（断言该行只有 1 个按钮，就是「更换 Key」） |

「password 类型」和「显示后四位」这两条直接冲突——password 框里只会显示圆点。做法是输入框旁边另起一行明文显示 `当前：****1234`，加一个「更换 Key」按钮清空输入框。两条规则同时满足，实测两处都断言到了。

### B. 管理页 · Embedding（对应完成标准 6 / 7 / 8 的前端半边 / 9）

- 路由选了 `/admin/embedding`，走的是**已有的** `RequireAdmin`，和 `/admin/invites`、`/admin/users` 同一个守卫，没有另写角色判断
- 非管理员实测：导航里没有入口；直接敲 `/admin/embedding` 被挡回首页。**这只是体验，真权限在服务端**，403 归 api 验
- EMBED_NOTICE 逐字比对通过
- 换模型确认：断言了三句都在——「全站重新索引」「OCR + 标签」「不中断」。它是页面内的 `role="alertdialog"` 区块，不是浏览器 confirm
- `EMBED_MODEL_CHANGED`（409）**没有被当成错误弹出来**，断言了此时页面上 `.error` 数量为 0；确认后带 `confirmReindex: true` 重发，保存成功
- 重建进度在管理页上和配置表单并列可见，**不是模态**；计数直接渲染服务端返回的 `done/total/stale/failed`（实测 830/1200、stale 370、failed 2），前端不自己累加。只在 `running` 为真时轮询（5 秒，自重排的 `setTimeout`，卸载清理）

### C. 测试连接控件（对应完成标准 2 / 3）

这是这次花时间最多的地方。

- **逐项列出，不是红绿灯**：视觉 4 项、Embedding 2 项，每项 `✓ / ✗ / —`，不靠颜色区分
- **三态**：`null` 渲染成「未探测」而不是「不支持」。实测失败那一轮里 4 项中有 3 项是 null，断言它们显示的是「未探测」
- **原样、不截断**：拿界面上 `<pre>` 的 `innerText` 长度和接口返回的 `rawResponse.length` 直接比，637 == 637。失败时默认展开，可复制。CSS 上 `max-height: 320px` 是**滚动**不是截断，注释里写了
- **200 不当错误**：`ok:false` 走的是 `phase: 'done'`，和 HTTP 失败的 `phase: 'failed'` 是不同分支，所以 `rawResponse` 不会被丢掉
- **保存闸门**：未测试禁用、测试不通过仍禁用、改任一字段立刻清掉测试结果并重新禁用——三条都断言到了
- 服务端 `CONFIG_TEST_REQUIRED` 的兜底也测了：错误里带 `requestId`，且前端会**作废本地测试状态**逼用户重测（不是只弹个提示了事）

有一处自己改过：`willTruncate: true` 一开始渲染成 `✗ 需要截断到 1024 维`，这是错的——**支持 MRL 的模型截断基本无损**（§9.6），标成 ✗ 会让管理员以为选错了模型，而系统并不知道它支不支持 MRL。改成从 ✓/✗ 清单里移出来，单独一行中性陈述。

### D. 样式

只写了布局：flex / grid、尺寸、间距、`display`、`overflow`。没有颜色、字体、阴影、圆角。手机 390px 下实测输入框不横向溢出，按钮触摸目标 ≥ 44px。

### E. 类型 —— ⚠️ 这是本端的阻塞点

接口类型**没能从 Hono RPC 派生**，因为六个端点还没挂进 `app.ts`。

处理方式：所有待定形状集中在 `src/lib/api-config.ts` **一个文件**里，文件头写明它们是阻塞态的临时占位、不是本端自留的第二份契约，并逐条列出了将来替换它们的 `InferResponseType` 单行写法。其余文件里的类型都是 web 自己的视图类型，不描述接口。

**api 挂上路由后要回来做这件事**，在那之前**没有任何编译期保证字段名是对的**——替身是我照 SPEC 写的，我照着自己的理解写替身、再用替身验自己，这个环里没有 api 的真实实现。`source` 的两个取值、能力位的 null 语义都是按 SPEC 文字理解的。

### 提请总管裁定：副通道那一句文案

按要求逐字照抄、一个字没删，但要把问题提出来：

VISION_NOTICE 里有「副通道建议配一个内容策略更宽松的供应商」，而本任务明确不做副通道表单，**界面上没有任何对应的输入框**。用户读到这句话会去找「副通道」在哪里，找不到。

我的判断是这句话目前会造成困惑，但**不认为该删**——它说明的是这套设计为什么长这样，删了以后 §9.5 落地时也不会有人记得加回来。倾向的处理是在文案底下加一行**界面说明**（不是改文案）：「副通道的配置入口尚未开放」。这一行是 web 的界面文字不是契约文案，但因为紧贴契约文案，**没有自作主张加，等裁定**。

### 本次改了什么

新增 `src/lib/api-config.ts`、`src/features/settings/`（8 个文件）、`src/routes/settings.tsx`、`src/routes/admin-embedding.tsx`；改了 `src/App.tsx`（两条路由 + 两个导航入口）和 `src/styles.css`（追加布局块）。没有动 SPEC。

### 还没做 / 下一步

1. **api 挂上 `/api/v1/config/*` 与 `/api/v1/admin/reindex*` 之后**，把 `api-config.ts` 的占位类型换成 `InferResponseType`，重跑 typecheck——字段名对不对那时才知道
2. 真接口联调：`ok:false` 的真实形状、`CONFIG_TEST_REQUIRED` / `EMBED_DIM_TOO_SMALL` / `EMBED_MODEL_CHANGED` 的真实 code 与 409 状态、重算进度的真实推进
3. `EMBED_DIM_TOO_SMALL` 的保存失败路径**只走了 `describeSaveError` 的代码分支，没在浏览器里实跑**（替身没造这一路）
4. 副通道文案待裁定（见上）

## 联合验收

**未通过，任务保持 `in_progress`。** 2026-09-18 · 总管

不通过的原因只有一条，在下面的「剩余工作」里：**web 的接口类型还是手写的占位**，
两端从未真的对接过。其余部分都验过了。

### 总管这边实测到什么

| 项 | 结果 |
|---|---|
| `cd web && npm run typecheck`（闸门） | 0 错误 |
| `cd api && npx tsc -p tsconfig.json --noEmit` | 0 错误 |
| `cd api && npm run test:unit` | 13 文件 / **123 passed** |
| `cd api && npm test`（含集成） | **没能在总管这边跑**——本机没有 Postgres 也没有 Docker，`global-setup` 直接 ECONNREFUSED |
| 凭据扫描 | 差异里没有 `.env`、没有形如 key 的串、没有 `CONFIG_ENC_KEY` 取值 |
| 契约文案 | `settings-copy.ts` 两段与 `settings-ux.md §4` 逐字比对一致 |

**「259 passed」是 api 执行者的测量，不是我复核过的数字**——我这边跑不了集成套件。
单测和两条 typecheck 是我自己跑的。

### 三条裁定

**1. 副通道那句文案：按你的倾向办，加那一行界面说明。**

提得对，而且处理方式也对——发现契约文案会造成困惑时**把问题提上来而不是自己删**，
这正是「文案是契约」该有的走法。裁定：

- 契约文案一个字不动，那一行界面说明放在 `<pre>` **外面**，不要混进契约文本里，
  否则下一次逐字比对就会失败，而那个比对是这段文案唯一的防线
- 措辞限定在「入口尚未开放」这类**界面可用性**陈述，不要替契约解释「为什么建议配副通道」
- 这一行的寿命绑在 [SPEC §9.5](../spec/09-decisions.md) 上：副通道表单落地时删掉它。
  已经记进下面的遗留项，不靠谁记得

**2. `PUT /config/embed` 多返回的两个字段：留下，但这个顺序错了。**

`reindexTriggered` / `reindexEnqueued` 不在 §6.5.3 里，是实现先长出来的。
`api/AGENTS.md §2` 写得很直白：**响应里多一个字段，先改 SPEC**。这次是我补的
（已进 §6.5.3），下次请在实现之前提出来——一个没进 SPEC 的字段，web 那边不知道
它存在，等于白写。

留下的理由：它回答的问题 `GET /reindex/status` 确实答不了——status 只能说
「此刻有没有重算在跑」，分不出那是不是这次保存造成的。

**3. `willTruncate` 从 ✓/✗ 清单里移出来：判断正确，这是 web 自己的地界。**

§9.6 明写支持 MRL 的模型截断基本无损，而系统并不知道用户填的模型支不支持 MRL——
把它标成 ✗ 是在报告一个系统没有的知识。**呈现方式归 web**（`web/AGENTS.md §2`），
不必等裁定；契约管的是 `willTruncate` 这个字段的含义，不管它画成什么。

### 值得记下来的两件事

**探测图的生成不是跨机器确定性的。** 字幕走 SVG `<text>` + 系统字体，
没有中文字体的机器重跑生成脚本会**静默**渲染成豆腐块——那时探测图上没有可读文字，
`vocabCompliant` 会把好模型判成不合规，而且不报错。记录在
`scripts/gen-probe-image.ts` 的文件头里，就在会去重跑它的人眼皮底下，这个位置是对的。

**「这个 bug 的表现是没有表现」被当成了断言设计的出发点。** `isVisionConfigured()`
那条如果只断言 `tagStatus === 'ok'`，拿部署方的 key 跑通也是 ok；`rawResponse`
那条如果只断言「出口只有一个」，等于在验代码结构而不是验行为。两条都往下挖到了
可观测的事实（出站 `Authorization` 头、字节级全等）。这是本次最值得留下的做法。

### 剩余工作（web 收尾，阻塞已解除）

六个端点已链式挂进 `app.ts`，RPC 能推类型了。web 端还差：

1. `src/lib/api-config.ts` 的占位类型换成 `InferResponseType`，重跑闸门——
   **字段名对不对，那一刻才第一次有人知道**。现在两端各有一份对 SPEC 的理解，
   中间没有任何东西把它们对过
2. 真接口联调：`ok:false` 的真实形状、三个错误码的真实 code 与状态码、重算进度的真实推进
3. `EMBED_DIM_TOO_SMALL` 的保存失败路径在浏览器里实跑（此前替身没造这一路）
4. 副通道那行界面说明，按裁定 1 加上

做完这四条再来一次联合验收。**不要为了让 1 编译通过就去改 api 的响应形状**——
形状以 SPEC §6.5.3 / §6.5.4 为准，对不上的一方改自己那边；如果发现 SPEC 本身写错了，
按 [SPEC §8.4](../spec/08-collaboration.md) 上报，不要就地改成代码的样子。


## web 端验收 · 收尾轮

回填时间 2026-09-18 · web 执行者。对应上面「剩余工作（web 收尾，阻塞已解除）」的四条。

### 实测环境 —— 这一轮和上一轮的区别

上一轮是「我照 SPEC 写替身、再用替身验自己」。这一轮**替身只剩模型供应商那一层**：

| 这一轮是真的 | 这一轮仍是假的 |
|---|---|
| api 服务（`npm run dev`，:3000）、Postgres（容器 `mememio-db`） | 模型上游 —— 一个本地 OpenAI 兼容替身（127.0.0.1:4100） |
| 六个端点、路由、鉴权、角色、错误码、requestId | |
| 重建索引队列与 worker（真的跑完了 6 条） | |
| 真 Chromium（Playwright 1.62.1），桌面 1280×900 + 手机 390×844 | |

**为什么上游还是假的**：`.env` 里是用户付费的真 key，跑一轮验收要打几十次打标和 embedding 请求。
换成本地替身之后，**除了模型厂商，链路上其余全是真的**；而且 768 维模型、上游 401、词表越界、
json 模式失效、单图限制这几条路径拿真供应商根本凑不出来，替身反而让它们成了确定性用例。

替身还原了 OpenAI 的报错体型（`{error:{message,type,param,code}}`），
按路径前缀分 6 种模式：`good` / `weak` / `small` / `badkey` / `offvocab` / `refuse`。

数据是自己铺的：两个测试账号（`webtest-adm` 提权为 admin、`webtest-mem` 普通用户，各自走真注册 + 邀请码），
6 条 `webtest/` 记录带 1024 维旧向量和 `embed_model='legacy-embed-model'`，
好让 `hasEmbeddedMemes()` 为真、409 这条路走得到。验收跑完全部清掉，见文末。

**结果：81 条断言，80 过 1 挂。** 挂的那条是真的契约偏差，不是测试写错，见下面 §8.4 上报。
闸门：`npm run typecheck` 0 错误，`npm run build` 通过（71 modules，dist 233.55 kB）。

### 1. 类型换成 `InferResponseType` —— 做完了，而且真的抓到了东西

六个占位全换成 RPC 派生：

```ts
export type VisionConfig      = InferResponseType<typeof api.api.v1.config.vision.$get>
export type EmbedConfig       = InferResponseType<typeof api.api.v1.config.embed.$get>
export type EmbedConfigSaved  = InferResponseType<typeof api.api.v1.config.embed.$put>
export type VisionTestResult  = InferResponseType<typeof api.api.v1.config.vision.test.$post>
export type EmbedTestResult   = InferResponseType<typeof api.api.v1.config.embed.test.$post>
export type ReindexStatus     = InferResponseType<typeof api.api.v1.admin.reindex.status.$get>
```

第一次编译**没过，2 个错误**，而且两个都是我上一轮的占位写错了：

- 我给两路测试结果都写了 `rawResponse` **和** `rawError`
- api 实际是**不对称**的：视觉只回 `rawResponse`，embedding 只回 `rawError`

回查 SPEC §6.5.1 的两段示例，**api 是对的，对称是我自己脑补的**。
所以改的是 web：`TestResultPanel` 的两个 raw 参数改成可选，缺的那一个不渲染空壳，
两处调用点各自只传自己有的那个。**没有动 api 的响应形状。**

这正是上一轮写的那句「在那之前没有任何编译期保证字段名是对的」——保证一接上，立刻就抓到了偏差。

**唯一还是手写的类型是 `ConfigInput`**（请求体，不是响应）。
原因写在文件里：`routes/config.ts` 的 `readProviderInput` 是手工校验、没挂 validator，
`InferRequestType` 推出来是 `unknown`。它**不享受**「api 改字段名 → web 编译失败」这个保证，
这一点在文件头写明了，不假装它和上面六个同级。

### 2. 真 api × 真浏览器联调 —— 本轮唯一真正要证明的事

81 条断言的分布：视觉 A1–A34、Embedding / 重建 B1–B30、权限 C1–C6、手机 M1–M11。挑要紧的说：

**key 不出响应（硬边界，SPEC §5.3）** —— A18/A28/A29/A30/A31：
填一把 `sk-...` 进去保存，然后在 `PUT` 和 `GET` 的**响应全文里搜我填的那把 key**，搜不到；
回来的是 `****` + 后四位，后四位和我填的对得上；界面也只回显后四位。
不是「看了一眼觉得没有」，是对响应体做的子串断言。

**`ok:false` 走 HTTP 200**（A12/A13/A14） —— 用 `weak` 模式让 json 模式失效，
服务端返回 200 且 `ok:false`，前端走 `phase:'done'` 而不是 `'failed'`，页面上 `.error` 计数为 0。
这条上一轮是替身喂的，这一轮是服务端真判出来的。

**原始输出不截断**（A16/M3/M4） —— 界面 `<pre>` 的 `innerText` 和接口 `rawResponse` 做**长度全等**比对，
桌面和手机各一次。`max-height: 320px` 那条实测是滚动（`scrollHeight > clientHeight` 且 `overflow-y: auto`），
不是截断，全文在 DOM 里、复制按钮拿到的是全文。

**三个错误码都是服务端真的发出来的**，不是替身摆的：
`CONFIG_TEST_REQUIRED`（A33 改字段后强制重测）、
`EMBED_DIM_TOO_SMALL`（B6，见下一条）、
`EMBED_MODEL_CHANGED` 409（B13/B14，桌面 B13、手机 M8 各拦住一次）。

**重建索引是端到端跑完的**（B18→B25）：确认换模型 → 服务端排队 6 条 →
worker 真的把 6 条重新算了一遍 → `stale` 从 6 掉到 0 → 界面跟着显示「当前没有进行中的重建」（B28）
→ 停止轮询（B29，6.5 秒内零请求）。
中途挂过一次：我原来只等 20 秒，而 worker 是**定时轮询队列**不是入队即跑，api 日志里
入队 10:29:02、六条「重算向量完成」到 10:29:49，约 47 秒。等待放宽到 120 秒后稳定通过——
**是我的等待写短了，不是服务端慢**。

**权限（C1–C6）**：普通用户导航里没有入口、敲 `/admin/embedding` 被挡回首页，
同时**直接打 `/api/v1/config/embed` 服务端真的回 403 `FORBIDDEN`**。
前端那道守卫是体验，真权限在服务端——这一条现在有证据了。

**手机 390×844（M1–M11）**：`/settings` 与 `/admin/embedding` 均无横向溢出（展开测试结果后再测一次，
M6），换模型确认区可见且不撑出屏幕，取消后收起且配置未变。

桌面全程无未捕获异常（B30）。

### 3. `EMBED_DIM_TOO_SMALL` 在浏览器里实跑过了

用替身的 `small` 模式（真返回 768 维）：
测试连接 → 界面显示实测维度 768（B4，**数字来自上游真实返回**，不是造的）→
逐项说清为什么不够（B5）→ 点保存 → 服务端 `EMBED_DIM_TOO_SMALL`（B6）→
错误里带 `requestId`（B7）→ 前端**作废本地测试状态逼用户重测**（B8），不是弹个提示了事。

上一轮写的「只走过代码分支」这条现在销掉了。

### 4. 副通道那一行 —— 按裁定 1 加上了

`VisionSettings.tsx:126`，在 `<pre>` 契约块**外面**：`副通道的配置入口尚未开放。`

三条裁定条件都有断言守着：契约文案一字未删（A6，仍与规则文件逐字全等）、
新增那行确实在（A7）、**它在 `<pre>` 之外没混进契约文本**（A8，直接断言 `<pre>` 的
`innerText` 里不含这句话）。措辞只讲界面现状，没有替契约解释为什么建议配副通道。
寿命绑 §9.5 这件事写在代码注释里。

### ⚠️ 按 SPEC §8.4 上报：`reindexEnqueued` 的类型和 SPEC 对不上

**这是本轮唯一挂掉的断言（B20），也是唯一需要总管裁定的事。**

| | |
|---|---|
| SPEC §6.5.3 | `reindexEnqueued`（是否真的排进了重算队列，**库里没有向量时为 false**）—— 布尔 |
| 实际返回 | `typeof = number`，实测值 `6`（= 排进队列的条数） |
| 出处 | `api/src/routes/config.ts:150` `reindexEnqueued: outcome.enqueued`，而 `SaveEmbedOutcome.enqueued` 是 `number` |

**没有就地改成代码的样子，也没有改 SPEC**，按交接段里的 ⚠️ 原样上报。

需要裁定的是哪边改：
- 若 SPEC 为准 → api 改成 `outcome.enqueued > 0`
- 若实现为准（返回条数确实比布尔多一点信息）→ 改 SPEC §6.5.3 的字段描述，
  并且**字段名该跟着改**，`...Enqueued` 读起来就是个布尔，叫 `reindexEnqueuedCount` 才名副其实

web 这边**两种都不用改**：`updated.reindexEnqueued` 只做真值判断，`0` 和 `false` 行为一致，
`6` 和 `true` 行为一致。所以这条不阻塞 web，但**它是个契约偏差，不该因为碰巧不出事就留着**。

### 顺带发现并修掉：全局导航条的触摸目标只有 26px

M5 第一轮挂了。390×844 实测：导航里中文链接靠行高凑到 51px，
**拉丁字母的（品牌名 `Mememio`、`Embedding`）只有 26px**，低于 `styling.md` 的 44×44px。
同一条导航上两种可点尺寸，纯粹是字形高度差撞出来的。

修法是 `styles.css` 追加的布局规则（`display:inline-flex` + `align-items:center` + `min-height:44px`），
**只有尺寸，没有颜色 / 圆角 / 过渡**，符合 web/AGENTS.md §5。
修完实测：`Mememio:44 | 浏览:51 | 导入:51 | 设置:51 | 管理:51 | Embedding:44`，全部达标。

M5 的断言本身也重新划了范围：只对本任务的两个页面（`.settings-page, .admin-page`）做硬断言，
全局导航条的高度**另打印成 NOTE**——不把别的任务的页面拖进本任务的红绿灯里。

### 本次改了什么

| 文件 | 改动 |
|---|---|
| `src/lib/api-config.ts` | 六个占位类型 → `InferResponseType`；`ConfigInput` 保留手写并说明原因 |
| `src/features/settings/TestResultPanel.tsx` | 两个 raw 参数改可选（两路输出不对称），缺的不渲染空壳 |
| `src/features/settings/EmbedSettings.tsx` | 保存后的提示改为**读 PUT 回执**（`reindexTriggered` / `reindexEnqueued`）而不是本地推断；去掉传错的 `rawResponse` |
| `src/features/settings/VisionSettings.tsx` | 加副通道界面说明（`<pre>` 之外）；去掉传错的 `rawError` |
| `src/styles.css` | 导航触摸目标 44px（纯尺寸） |

**没有动 SPEC，没有动 `api/`，没有动「api 端验收」小节。**

### 还没做 / 已知限制

1. **`reindexEnqueued` 待裁定**（上面 §8.4 那条）。web 不阻塞，但偏差还在
2. **模型上游是替身**。api / DB / 队列 / 权限 / 错误码全真，但「真供应商在真实网络下的行为」
   没验过——超时、限流、流式截断、各家 `error` 体型差异，这些这一轮一条都没碰
3. 手机是 Playwright 的 390×844 视口 + 触摸模拟，**不是真机**。
   本任务两个页面不涉及复制 / 分享路径，所以没有走真机那道要求；复制 / 分享改动时仍必须上真机
4. 副通道那行界面说明的寿命绑 §9.5，落地时要删（注释里写了）
5. `web/` 没有测试框架，这 81 条断言在临时驱动脚本里，**跑完已删，没有进仓库**。
   下次要复现得重写。要不要把它沉淀成常驻的 e2e，归总管定

### 验收环境清理

临时服务（api dev / vite / 替身）与两个临时 `.mjs` 文件已停止并删除；
数据库里 6 条 `webtest/` 记录、两个 `webtest-*` 账号、两个邀请码、
替身产生的配置与测试记录已按 `where` 精确清除。`web/` 的 git status 只剩上表五个文件。

## 联合验收 · 第二轮（总管，2026-09-18）

**结论：仍未通过，任务保持 `in_progress`。** 差的只剩一件事，见「§8.4 裁定」。

这一轮和上一轮性质完全不同。上一轮不通过是因为**两端从来没有真的对接过**，那是根本性的；
这一轮对接跑通了，剩下的是一个字段名。

### 我自己复核的（不是转述两端的测量）

| 项 | 结果 |
|---|---|
| `cd web && npm run typecheck` | 0 错误 |
| 契约文案对 `settings-ux.md §4` | `VISION_NOTICE`（391 字）/ `EMBED_NOTICE`（242 字）逐字全等 |
| 副通道那行在 `<pre>` 之外 | 是（`VisionSettings.tsx`，`<pre>` 闭合后另起 `<p>`） |
| `styles.css` 的新规则只有尺寸 | 是，`display` / `align-items` / `min-height`，无颜色圆角过渡 |
| 工作树无残留 | 无未跟踪文件，临时 `.mjs` 与替身脚本都不在仓库里 |
| 凭据扫描（全 diff） | 干净；`.env` 未被跟踪 |
| **开发库残留** | **全部业务表 0 行**，只剩 `__drizzle_migrations`。`embed_config` 也是 0——没有留下带密文 key 的配置行 |

「81 条断言 80 过」是 web 执行者的测量，我复现不了（驱动脚本已删）。**这是一个我接受但没有验证的数字**，
和上一轮 api 那个「259 passed」性质一样。任务归档后这两个数字都不再有对应的可执行物，
引用它们时要知道这一点——这也正是下面那条 e2e 裁定的由来。

### 两端各自抓到了对方的错，这一轮的价值主要在这里

- api 的不对称响应（视觉只回 `rawResponse`、embedding 只回 `rawError`）是**对的**，
  web 上一轮的占位脑补了对称，接上 RPC 第一次编译就炸了两处。改的是 web，没动 api——处理正确。
- `reindexEnqueued` 的类型偏差是 web 的真断言抓出来的，且**没有就地改成代码的样子**，按 §8.4 上报。

这两条合起来说明：上一轮「不通过」的判断是对的。当时两端各自全绿，而真接上之后立刻暴露两处偏差。
**全绿不等于对接过。**

### ⚠️ §8.4 裁定：`reindexEnqueued` → `reindexEnqueuedCount`，实现为准

**先认一件事：SPEC 那句是我写错的，不是 api 写错的。** 上一轮我给这两个字段补 §6.5.3 时，
是照着「它该表达什么」写的，写下「库里没有向量时为 false」却没有回看 `SaveEmbedOutcome.enqueued`
的类型。api 的实现从头到尾一致，偏差出在契约那一侧。

裁定**取实现的数字，但字段名必须跟着改**：

- 条数比布尔多一份已经算出来的信息，退化成布尔是白扔。理由写进 SPEC §6.5.3 了，
  含它和 `status.stale` 为什么不是一个东西。
- 但 `...Enqueued` 读起来就是布尔。留着这个名字返回数字，下一个客户端写 `=== true`
  会**静默**判错——web 现在只做真值判断纯属侥幸，不是设计。

已改 SPEC §6.5.3（表格 + 两条理由）。**api 端还剩一次很小的收尾**，见下。

### 剩余工作（api，一次收尾）

1. `SaveEmbedOutcome.enqueued` 与 `PUT /config/embed` 的响应字段一并改名为 `reindexEnqueuedCount`
   （`services/ai-config.ts`、`routes/config.ts:150`）。
2. 补一条断言：换模型且库里有 N 条向量时，该字段等于 N（不是 `true`）。
   现有测试如果只断言真值，改成断言数字——**否则这次改名等于没改**，下次退回布尔照样全绿。
3. 两条闸门重跑。

web **不需要改**：`updated.reindexEnqueued` 改名后编译会直接报错，改一处变量名即可；
真值判断的行为在 `0` / `N` 下和现在完全一致。改完 web 顺手把提示改成带条数更好，但不强制。

### 裁定：要不要把 81 条断言沉淀成常驻 e2e

**要，但不在本任务里做，也不是全部 81 条。**

`web/` 至今没有任何测试框架（`package.json` 只有 `dev` / `build` / `preview` / `typecheck`）。
这已经是第四个用一次性脚本跑完几十条断言、跑完即删的任务了。代价不是「这次没留下」，
而是**每一轮都要重写驱动**，而且归档文档里的数字全都不可复现。

但也不能整包沉淀：

| 值得留 | 不值得留 |
|---|---|
| 硬边界（key 不出响应的子串断言）、三个错误码路径、403、重建端到端 | 布局类（44px、无横向溢出）——视觉风格还没定稿（`web/AGENTS.md §5`），留下来只会因为装饰改动变红 |
| **那个六模式的模型上游替身**（`good`/`weak`/`small`/`badkey`/`offvocab`/`refuse`） | 依赖手铺账号和 6 条 `webtest/` 记录的那部分，除非连种子一起沉淀 |

替身是这批东西里最该留的一件：768 维、上游 401、词表越界这些路径**拿真供应商凑不出来**，
它把它们变成了确定性用例。它已经被删了，但设计还在上面的 web 验收里，可以照着重建。

排期：**排在 SPEC §6.4（编辑/软删/restore/retag/查重）和部署之后。** 理由是项目已有的取舍——
测试设施不阻塞核心功能。已登记到任务板的「已知缺口」。

### 不阻塞但记在这里的限制

1. 模型上游全程是替身。真供应商的超时、限流、流式截断、各家 `error` 体型，一条都没验过。
2. 手机是 Playwright 视口不是真机。本任务两页不涉及复制 / 分享路径，所以够用；
   **动那条路径时仍必须上真机**（`web/AGENTS.md §6`）。
3. 副通道那行界面说明绑 SPEC §9.5，落地时删。已在任务板留条目，不只在代码注释里。


## api 端验收 · 收尾轮

回填时间 2026-09-18 · api 执行者。对应上面「剩余工作（api，一次收尾）」的三条。

### 1. 改名

| 位置 | 改成 |
|---|---|
| `src/services/ai-config.ts` `SaveEmbedOutcome.enqueued` | `reindexEnqueuedCount` |
| `src/routes/config.ts:150` 响应字段 | `reindexEnqueuedCount: outcome.reindexEnqueuedCount` |
| 换模型那条日志里的 `enqueued` 字段 | 一并改名 |

类型定义上写了「名字必须带 `Count`」的理由并引了 SPEC §6.5.3——不写的话，下一个嫌名字长的人
会把它改回去，而改回去是**静默**的：类型仍是 `number`，测试仍全绿，只有客户端写 `=== true` 时才炸。
日志字段跟着改是顺手的一件事：日志和响应叫同一个名字，查问题时不用在两个词之间换算。

**没有动 `POST /admin/reindex` 响应里的 `enqueued`。** 那是另一个端点的字段，不在这次裁定范围内，
SPEC §6.5.4 也没规定它的响应体；顺手改会再次碰倒 web 的编译。记在这里当作已知的命名不一致，
要统一的话该由总管一次裁定（它同样是数字、同样叫得像布尔）。

### 2. 补的断言 —— 以及我怎么确认它真的挡得住

新增一条集成用例（`tests/config-endpoints.test.ts`，该文件 20 → 21 条）：库里铺 **3 条**旧模型向量，
换模型 + `confirmReindex: true`，断言 `reindexEnqueuedCount` 等于 `3`，并且等于队列里实际多出来的行数
（`expect(body['reindexEnqueuedCount']).toBe(jobs.length)`），而不只是「大于 0」。

**为什么铺 3 条而不是沿用原来的 1 条**：N=1 时「排了多少条」和「排没排」返回的是同一个值（`1`），
断言分不出两种实现——裁定里担心的「改名等于没改」正是这种情况。

**这条断言真的会红吗**：把 `routes/config.ts` 临时改成 `reindexEnqueuedCount: outcome.reindexEnqueuedCount > 0`
（就是「退回布尔」那种回归）重跑，红了 3 条，都在「PUT /config/embed 的三道闸门」下：

| 用例 | 报错 |
|---|---|
| 第一次配置不算换模型，不触发重建 | `expected { source: 'user', …(8) } to match object …`（`0` ≠ `false`） |
| 库里有向量时换模型必须确认，409 之后带 confirmReindex 才过 | 同上（`1` ≠ `true`） |
| 回的是**条数**不是布尔：库里 3 条旧向量 | `expected true to be 3` |

改回来重跑，21 条全绿。**没有只跑绿的那一遍**——一条断言没见过它红，就不知道它钉住的是什么。

### 3. 两条闸门

- `cd api && npm test` → 26 个文件 **260 passed**（上一轮 259 + 本轮新增 1 条），耗时 23.7s；
  `npx tsc -p tsconfig.json --noEmit` 退出码 0、无输出。
- `cd web && npm run typecheck` → **2 个错误，都是预期内的**：

  ```
  src/features/settings/EmbedSettings.tsx(85,21): error TS2551: Property 'reindexEnqueued' does not exist
    on type '{ …; reindexTriggered: boolean; reindexEnqueuedCount: number; … }'. Did you mean 'reindexEnqueuedCount'?
  src/features/settings/EmbedSettings.tsx(90,19): error TS2551: （同上）
  ```

  按裁定这一处由 web 自己改（实际是 **两处引用**，85 和 90 行，不是一处）。**我没有动 `web/` 的任何文件。**
  顺带说明 RPC 的类型链路是通的：报错里 TS 打出的 api 响应类型已经是 `reindexEnqueuedCount: number`，
  说明改名确实传到了 web 这一侧，而不是只改了 api 内部。

### 本次改了什么

改了 `src/services/ai-config.ts`、`src/routes/config.ts`、`tests/config-endpoints.test.ts`（+1 条用例）。
**没有动 SPEC**（§6.5.3 由总管上一轮改过，本轮按它的口径对齐实现）。
没有动打标提示词、词表和检索参数，**没有跑评测集**——本轮不涉及影响打标或召回质量的部分。

### 还没做

上一轮「还没做 / 已知限制」那四条**一条都没变**（真供应商、大数据量重建、日志没有 `source` 字段、副通道）。
本轮新增一条：`POST /admin/reindex` 的 `enqueued` 与这次统一后的命名不一致，见上面第 1 节。

## 联合验收 · 第三轮（总管，2026-09-18）

**结论：仍未通过，任务保持 `in_progress`。** 剩两个单行改动，两端各一个，互不依赖，可并行。

### 这一轮我第一次自己跑通了 api 的集成测试

前两轮我都只能记「259 / 260 是执行者的测量，我复现不了」——本机没起 Postgres。
这一轮开发库容器在跑，我自己跑了：

| 项 | 结果 | 谁跑的 |
|---|---|---|
| `cd api && npm test` | **26 个文件 260 passed** | **我自己**（前两轮是执行者的测量） |
| `cd web && npm run typecheck` | **2 个错误，都在 `EmbedSettings.tsx` 85 / 90 行** | 我自己 |
| 凭据扫描 / 未跟踪文件 | 干净 | 我自己 |

那 2 个错误是**预期的、也是这一轮最有价值的证据**。TS 打出的类型里写着
`reindexEnqueuedCount: number`——说明改名确实经 Hono RPC 传到了 web 那一侧，
不是只改了 api 内部。骨架任务第 10 条那句「改字段名 web 编译失败」的保证，这是它第一次真的开火。

### 改名收尾：通过

三处都对（比裁定列的多改了一处日志字段——响应和日志同名，查问题不用换算，是对的）。
理由和 SPEC §6.5.3 写进了类型注释，这一条重要：不写的话，下一个嫌名字长的人改回去是**静默**的，
类型仍是 number、测试仍全绿，只有客户端写 `=== true` 时才炸。

断言那条做得比我要求的好。我只写了「断言等于 N」，执行者做了两件我没写的事：

1. **把用例从 1 条向量铺到 3 条**。N=1 时「排了多少条」和「排没排」返回同一个值（`1`），
   断言分不出两种实现——我裁定里说「否则这次改名等于没改」，而只改断言不改数据量，
   仍然等于没改。这个补丁是执行者自己看出来的。
2. **把退化实现跑了一遍确认断言会红**（临时改回 `> 0`，红 3 条，改回来 21 条全绿）。
   一条断言没见过它红，就不知道它钉住的是什么。

### ⚠️ 新裁定：`POST /admin/reindex` 的 `enqueued` → `enqueuedCount`

执行者没顺手改这个端点，只记进「还没做」等裁定——处置正确，它确实不在上一次裁定的范围里。

裁定：**改，而且现在就改。** 两条理由：

1. 同一个概念在两个端点上不能有两个名字。这个仓刚因为「一个叫得像布尔的数字」出过一次契约偏差，
   把同样的形状留在隔壁端点，等于把同一个坑再挖一遍。
2. 执行者担心「顺手改会再次碰倒 web 的编译」——**这一次不会**。
   `web/src/lib/api-config.ts` 的 `startReindex()` 只看 `res.ok`，**不读响应体**，
   `ReindexStatus` 派生自 `GET .../status` 而不是这个 POST。所以这个改动 web 侧零影响。

同时补了 SPEC §6.5.4：这个端点此前**响应体根本没写进契约**（返回 `{ enqueued, ...status }`），
属于「实现有、契约无」。现在写明是 `{ enqueuedCount, ...status }`，并写明 `enqueuedCount: 0`
不是错误而是幂等的正常结果。

### 剩余工作（两端各一行，可并行）

**api：**
1. `routes/admin.ts` 的 `POST /reindex` 响应字段 `enqueued` → `enqueuedCount`（SPEC §6.5.4 已改）。
2. 若现有测试断言了这个字段，跟着改名；没断言过就补一条**断言条数**的（同样别用 `> 0`）。
3. 两条闸门重跑。

**web：**
1. `EmbedSettings.tsx` 85 / 90 两处 `reindexEnqueued` → `reindexEnqueuedCount`，
   闸门回到 0 错误。行为不用改：`0` / `N` 的真值判断和现在完全一致。
2. 可选：那句「全站重建索引已排队」带上条数（「已排队 N 条」）。
   现在这个数拿得到了，不带白不带——但不强制，不带也不算没做完。

### 提交状态

api 的改名与新测试已由总管提交（见下方 commit）。**这次提交时 `web` 闸门是红的，2 行，已知。**
记在这里是因为 `git log` 里看不出「红是预期的」——跨端改名不可能有一个既不混两端、又两端全绿的中间提交，
红的那一刻正是保证在生效。

---

## web 端验收 · 第三轮改名收尾（web 执行者，2026-09-18）

闸门从红回到绿。**没重跑整套联调**（按交接段的要求），下面分清了哪些是这一轮实测的、哪些是沿用上一轮的。

### 闸门

| 项 | 结果 |
|---|---|
| `cd web && npm run typecheck` | **0 错误**（改之前：2 个，`EmbedSettings.tsx` 85 / 90） |
| `cd web && npm run build` | 通过，71 modules，`dist/assets/index-9dbQbQmN.js` 233.59 kB，1.09s |
| `grep -rn "reindexEnqueued[^C]" web/src` | 无残留 |

### 改了什么

| 位置 | 改动 |
|---|---|
| `EmbedSettings.tsx:85` | `updated.reindexEnqueued` → `updated.reindexEnqueuedCount > 0` |
| `EmbedSettings.tsx:90` | 同上（`onReindexTriggered()` 的触发条件） |
| `EmbedSettings.tsx:81-84` | 注释跟着改：写明「它是条数不是布尔，判真值写 `> 0`」，并写明它和下方进度条里的 `stale` 不是一个东西 |
| `EmbedSettings.tsx:87` | **（可选项，做了）** 「全站重建索引已排队 **N 条**，进度见下方。」 |
| `api-config.ts:32-35` | `EmbedConfigSaved` 的类型注释里还留着旧名和「为 false」的旧含义，一并改掉，并写上「别写 `=== true`，那是静默判错」 |
| `api-config.ts:123` | 同上，`putEmbedConfig` 的注释 |

两处真值判断的行为**完全等价**：`0` 假 / `N` 真，和改名前一致。这一轮没有任何行为变化，只有那句多出来的条数。

### 顺带修掉一处被 SPEC §6.5.4 改动放旧的注释

`api-config.ts` 的 `startReindex()` 上写着「SPEC §6.5.4 **没有规定**这个端点回什么」——
第三轮总管把响应体补进契约之后，这句话就成了假话。改成陈述现在的契约，并保留不读响应体的**真实**理由：

> `enqueuedCount` 是「这一次点击」排进去的条数，而这个按钮要显示的是全局进度，一律以
> `GET /admin/reindex/status` 为准。

**代码行为一字未动**，只是让理由重新成立。留着假理由的坏处是下一个人照它去推断契约。

### 这一轮实测了什么、没测什么

| | |
|---|---|
| 这一轮实测 | 只有两条闸门。没起 api、没起数据库、没开浏览器 |
| 沿用「收尾轮」 | 那 81 条断言里 B18–B30 覆盖的换模型 → 入队 → worker 消费 → `stale` 6→0 的整条链路 |
| **没有任何人在浏览器里见过**的 | 新那句「已排队 **6** 条」的实际渲染 |

最后一条是这一轮唯一的诚实缺口，说清楚它的大小：文案所在的分支就是收尾轮跑过的那一条
（当时它渲染的是不带条数的同一句），数据来自同一个回执字段，条数由 api 侧「断言等于 N」的用例钉住。
但**「代码路径跑过」不等于「那行字被人看见过」**——中文里 `${n}` 前后要不要空格、`6 条` 在窄屏上会不会
把整句挤断行，这些只有渲染出来才知道。下一次任何人为别的事起浏览器时，顺手看一眼这句就能补上。

### 阻塞

无。B20 那条偏差（`reindexEnqueued` 名实不符）由第二轮裁定、api 改名、这一轮 web 跟上，**到此闭环**。

---

## api 端验收 · 第三轮改名收尾（api 执行者，2026-09-18）

`POST /admin/reindex` 的 `enqueued` → `enqueuedCount`。范围就这一件事，没有顺带改别的端点。

### 1. 改了哪三处

| 位置 | 改动 |
|---|---|
| `src/routes/admin.ts:137-138` | `const enqueued` → `enqueuedCount`，响应体 `{ enqueuedCount, ...status }`（SPEC §6.5.4） |
| `src/routes/admin.ts:126` | 文档注释里的 `enqueued: 0` 跟着改——这句正是在解释「0 不是错误」，名字不同步等于注释在讲另一个字段 |
| `src/routes/admin.ts:128-130` | 新增一段：它是条数不是布尔、叫 `enqueued` 会让客户端写成 `=== true` 静默判错、同一概念在 `PUT /config/embed` 那边叫 `reindexEnqueuedCount` |

最后一条和 `SaveEmbedOutcome` 上那段是同一个用途：**让下一个人改回去时是有声的**。
两个端点的注释互相点名，谁被单独改短了，另一边的注释立刻变成假话。

**`services/ai-config.ts` 里的 `enqueueAllStale()` 和它的局部 `enqueued` 没动**：
那是函数内部的计数器，不是对外字段，SPEC §6.5.4 管的是响应体。函数名已经是动词（`enqueue...`），
返回值类型由签名的 `Promise<number>` 说明，改成 `enqueuedCount` 只是给局部变量加噪音。

### 2. 测试：三处跟着改名 + 补一条 N=3

跟着改名的三处（都已经是断言条数，不是 `> 0`）：

| 用例 | 断言 |
|---|---|
| 手动触发把过期的排进队列，重复触发是幂等空操作 | `first['enqueuedCount']` = 1，`second['enqueuedCount']` = 0 |
| 没配 embedding 模型时触发重建是空操作 | `res['enqueuedCount']` = 0 |

新增一条 `回的是**条数**不是布尔：3 条过期 → enqueuedCount === 3`，和 §6.5.3 那条同一个理由：
**N=1 的用例挡不住「排没排」那种退化实现**，因为两种实现在 N=1 时返回同一个 `1`。
断言写成 `toBe(jobs.length)` 而不是硬编码 `3`，把这个数和队列里实际多出来的行数绑在一起。
顺带断言了 `{ running, total, done, stale, failed }` 仍在同一个响应里——改名动的是 spread 前面那个键，
把 `...status` 碰掉是这种改动最容易出的事故，而且 web 不读这个响应体，线上不会有人替我们发现。

### 3. 两次退化实测：确认这两条断言各自挡的是什么

| 临时把 `routes/admin.ts` 改成 | 红了几条 | 报错 |
|---|---|---|
| `enqueuedCount: enqueuedCount > 0`（退回布尔） | **3 条** | `expected true to be 1` / `expected false to be +0` / `expected true to be 3` |
| `enqueuedCount: Math.min(enqueuedCount, 1)`（数字，但只表示「排没排」） | **1 条**，只有新用例 | `expected 1 to be 3` |

第二行是这次专门多跑的一次：它证明**新用例不是前三条的重复**。
退回布尔的话老用例也能拦住（`toBe(1)` 对 `true` 会红），但「数字退化成 0/1」只有 N=3 拦得住，
而那恰恰是改名之后最可能悄悄发生的走形——字段名还叫 `Count`，类型还是 `number`，只是数不对。

两次都改回来了（`grep -n "enqueuedCount" src/routes/admin.ts` 核对 137-138 行），再跑一遍全绿。

### 4. 两条闸门

| 项 | 结果 |
|---|---|
| `npx tsc -p tsconfig.json --noEmit`（api） | 退出码 0，无输出 |
| `cd api && npm test` | **26 文件 / 261 passed**，24.87s（上一轮 260，+1 条新用例） |
| `cd web && npm run typecheck` | **0 错误** |

web 闸门这次是绿的，两件事都对上了：总管说的「web 侧零影响」属实，上一轮那 2 个 TS2551 也确实已由 web 修掉。
我自己另外核了一遍零影响的依据，没有只信裁定：`startReindex()` 是 `Promise<void>`，只判 `res.ok`；
`ReindexStatus` 派生自 `GET .../status`。**我没有动 `web/` 的任何文件。**

### 本次改了什么

- `src/routes/admin.ts`：响应字段改名 + 两段注释
- `tests/config-endpoints.test.ts`：3 处断言跟着改名，新增 1 条 N=3 的用例（22 条）
- 没动 SPEC（§6.5.4 已由总管补好，本轮按它实现）
- 没动提示词、词表、检索参数，**没跑评测集**

### 还没做 / 留给别人的

1. **`enqueuedCount` 这个数目前没有任何人在界面上看见。** web 的 `startReindex()` 按契约只看 `res.ok`，
   理由正当（按钮要显示的是全局进度）。但这意味着这个字段现在只被测试和日志消费——
   不是问题，只是记一笔：将来若没人用，它的回归只有测试会发现。
2. 「已排队 N 条」那句文案的实际渲染仍然没有人在浏览器里见过（沿用 web 第三轮的诚实缺口，未闭合）。

## 联合验收 · 第四轮（总管，2026-09-18）

**结论：通过。状态转 `done`，整份归档。**

### 闸门（这一轮全部由我自己跑，不是转述）

| 项 | 结果 |
|---|---|
| `cd api && npm test` | **26 文件 261 passed** |
| `npx tsc -p api/tsconfig.json --noEmit` | 退出码 0 |
| `cd web && npm run typecheck` | **0 错误**（上一轮是预期内的红 2 行） |
| 旧名残留扫描（`api/src` + `web/src`） | 无 |
| 凭据扫描 / 未跟踪文件 | 干净 |

### 改名闭环：通过

两端各自都做了比交接段多的事，而且多得对：

- api 把 `:126` 那句文档注释一并改了——那句正是在解释「`0` 不是错误」，
  名字不同步等于注释在讲另一个字段。新加的一段和 `SaveEmbedOutcome` 上那段**互相点名**，
  谁被单独改短了另一边立刻变成假话。这是把约束绑进代码而不是绑进人的记忆。
- api 没动 `enqueueAllStale()` 的局部变量。**这个克制是对的**：§6.5.4 管的是响应体，
  局部计数器改了只是噪音。改名的边界就该停在对外形状上。
- 新用例顺带断言了 `{ running, total, done, stale, failed }` 还在同一个响应里。
  改名动的正是 spread 前面那个键，碰掉 `...status` 是这种改动最容易出的事故，
  **而 web 不读这个响应体，线上没有人会替我们发现**。
- 多跑的那次退化实测（`Math.min(count, 1)`：字段名还叫 `Count`、类型还是 `number`、只是数不对）
  只有 N=3 的新用例拦得住，红 1 条。它证明了新用例不是前三条的重复。
- web 把 `api-config.ts` 里两处**注释**里的旧名和「为 false」的旧含义一并改掉，
  还修了 `startReindex()` 上被我这一轮改 SPEC 弄成假话的那句（「§6.5.4 没有规定这个端点回什么」）。
  注释里的旧契约不会让编译失败，只会让下一个人读到错的东西。

### 这个任务真正留下的东西

十项「做完的标准」全部有实测支撑。但回头看，这个任务的价值不在那十项，在三件事：

1. **「两端各自全绿」被证明了不等于「对接过」。** 第一轮两端都交了绿，
   真接上之后立刻两处偏差（`rawResponse`/`rawError` 不对称、`reindexEnqueued` 名实不符）。
   当时判「不通过」是这个任务里最值的一次裁定。
2. **「改字段名 web 编译失败」这条保证第一次真的开火。** 第三轮 web 那 2 行红，
   TS 打出的类型里写着 `reindexEnqueuedCount: number`——保证不是文档里的一句话了。
3. **契约偏差是我自己写出来的。** `reindexEnqueued` 那句「库里没有向量时为 false」
   是我补 SPEC 时照着「它该表达什么」写的，没回看实现的类型。
   AGENTS.md §4 防的是「把 SPEC 改成代码的样子」，但反过来「把 SPEC 写成想象的样子」
   一样会造出偏差，而且更难发现——它看起来完全像一份正常的契约。

### 转出的遗留项（已登记到任务板）

1. **「已排队 N 条」那句话没有人在浏览器里见过。** 代码路径在收尾轮跑过（当时渲染的是不带条数的同一句），
   条数由 api 侧「断言等于 N」的用例钉住，但 `${n}` 前后的空格、窄屏会不会挤断行，只有渲染出来才知道。
   web 本端，下次为别的事起浏览器时顺手看一眼。
2. **`enqueuedCount` 目前没有任何界面消费它**（`startReindex()` 按契约只看 `res.ok`，理由正当），
   它现在只被测试和日志消费——不是问题，但它的回归只有测试会发现。
3. **模型上游全程是替身。** 真供应商的超时、限流、流式截断、各家 `error` 体型，一条都没验过。
4. **手机是 Playwright 视口不是真机。** 本任务两页不涉及复制 / 分享路径；动那条路径时仍必须上真机。
5. 副通道那行界面说明绑 SPEC §9.5（已在板上）；`web/` 常驻 e2e（已在板上，排在 §6.4 和部署之后）。
