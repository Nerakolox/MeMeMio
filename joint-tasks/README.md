# 任务板

需要 `api` 和 `web` 同时动的事情放这里。判断标准不是工作量，是**「一端先合入会不会让另一端坏掉」**。什么时候必须开、什么时候不必，见 [SPEC §8.2](../spec/08-collaboration.md)。

**单端任务也登记在这块板上**，标注「api 单端」或「web 单端」，文件里只写那一端的小节。理由：执行者的开工读取只指向这里（见 `api/AGENTS.md §1`、`web/AGENTS.md`），本端任务写在别处等于没人会读到它。单端任务没有联合验收，本端验收通过即可归档。

命名 `YYYY-MM-DD-主题.md`。完成后移到 [`_archive/joint-tasks/`](../_archive/joint-tasks/README.md)，**不要删除**——它记录了这个决定是怎么来的。

## 状态

只有三个主状态，阻塞单列：

| 状态 | 含义 |
|---|---|
| `planning` | 契约还在讨论，**不写实现代码** |
| `in_progress` | 契约已转 `accepted`，两端在实现 |
| `done` | 联合验收通过（单端任务：本端验收通过），准备归档 |

各端只回填**本端**的任务和**有依据的**验证结果。没跑的测试不要写成跑过了。

## 当前任务

**核心功能优先，优化任务边用边迭代。** 词表、评测集、供应商探测不阻塞核心功能开发，先用 `proposed` 版本落代码，结果随使用积累后持续改进。

| 任务 | 状态 | 性质 |
|---|---|---|
| [运行参数：并发上限进管理页，改完不用重启](2026-09-23-runtime-config.md) | `in_progress` | **跨端**——产品负责人 2026-09-23：「应该放在设置页面作为一个配置项，可以动态变化。」**2026-09-23：两端实现已落，SPEC 四处转 `accepted`**（§3.3 权限矩阵 +1 行、§5.6 / §6.5.5 / §9.26 新增；另补 §9.9 第三块文案——它此前只活在本任务文件里）。api `npx vitest run` **425/425**（新增 26 条，**既有断言一条没改**）；web 在真 Chrome 1264×805 / 390×844 两档过了几十条断言，`typecheck` + `build` 过，并用一次故意的 `TS2353` 证明 RPC 类型链没退化成 `any`。**未转 `done`**：web 侧全部断言对着**按 api 实现逐字校准的替身**，两端从未对话——那正是[模型配置任务](../_archive/joint-tasks/2026-09-16-ai-config.md)第一轮判不通过的状态（真接上后立刻暴露两处偏差）。解除路径与真接上时要盯的三条见任务文件「总管裁定」。**并且本次没有量过真实提速**——机制做出来了、机制本身也验过了，但「调大之后确实更快」没有人量过，**不要读成「速度问题已解决」**。**症状**：四个并发上限是模块级常量（`worker.ts:28` 打标 2 / `:36` 每用户在途 1、`import.ts:37` 导入 2、`image/constants.ts:40` ffmpeg 2），而它们是**机器规格相关**的数，改一次要改代码 + 重新部署。触发条件早就写在 SPEC §9.25 ③ 里：`PER_USER_INFLIGHT = 1` 让**单用户库**的吞吐退化成 1 条/次、`CONCURRENCY = 2` 的第二个槽空转、75 张要 9–20 分钟。**三个选型已定**：①暴露打标 + 导入两条线共 4 项（重建索引并发、任务超时、轮询间隔**不做**——超时与 `STALE_RUNNING_MS` 有硬关系，可独立调节就会有人调反，表现是同一张图付两次钱）；②ffmpeg 上限**可下调、上调封顶 `min(核数, 16)`**（核数是唯一有依据的判据）；③**不新增状态端点**，生效证据复用 `GET /memes/tag-status` 的 `pending` 下降速率。**三处关键判断**：①**只能 admin**——并发是机器资源与 AI 账单，放普通用户设置页等于让任何 member 把机器打爆（§3.3 权限矩阵 + §9.9「全站一份的配置在管理页」）；②**导入那条线的真旋钮不是 `PIPELINE_CONCURRENCY` 而是 ffmpeg 的进程级 2 槽**（`probe.ts:19-34`），只暴露前者会「调完一点没变快」；③**动态生效不需要新机制**——本项目既有做法就是「不缓存、每次用之前查库」（vision / embed 两个先例），且**不能用进程内事件通知**（worker 不假设单副本，通知到不了别的进程）。**六个必须写进注释的陷阱**：①`setFfmpegConcurrency` 上调时**必须主动唤醒 `waiters`**，否则「调了没用」且不报错；②读配置必须在 tick 的 early-return（`:131`）**之前**，否则调高的生效延迟 = 一个在途任务的自然耗时（最长 90 秒）；③**不为了「立即生效」打断在途任务**（会留下永远停在 `running` 的行）；④默认值只有一个真源，等于默认的输入归一成 `NULL`；⑤**四个数都是「每进程」的**，多副本下 = 配置值 × 进程数，UI 不能说「全站」；⑥导入并发的收益低于预期（`import.ts:167` 的 sha256 是**同步**的，会挡事件循环）。**对选型 2 的一处修正**：越界**报错**（`VALIDATION_FAILED`）不静默截断。**契约五处**（§3.3 +1 行、§5.6 / §6.5.5 / §9.26 新增，以及后来补的 §9.9 第三块文案）。**不需要跑评测集**（不碰提示词/词表/检索参数）。 |
| [批量重打标 `POST /memes/retag` + 设置页「全部重新打标」](2026-09-22-retag-endpoint.md) | `in_progress` | **跨端**——产品负责人 2026-09-22：「我需要一个全部重新打标的按钮」。**症状**：词表升 v0.3.0 + 提示词从八个键改九个键，但**库里的图是旧提示词打的**——真库 75 张全部 `tag_status=ok`、全部有向量，而 `ratings` 非空 **0 张**（75 行 `tag_jobs` **全是 `done`**，所以调 `enqueueTagJob` 是静默空操作）。SPEC §6.4 早写着这个接口、标着「尚未实现」，§6.6.2 还写明不许在 §6.4 之外另造「重试」端点；本次兑现它。**六个选型已定**：跳过人工编辑过的（`skippedEditedCount`）、按钮放设置页紧挨「重建索引」、重打期间置回 `tag_status='pending'`、配置保持**上传者**口径并把偏差记进 SPEC（`useDefaultConfig` 不实现）、`memeIds[]` 与 `filter` 两个形状都做、**不新造进度端点**（轮询已有的 `tag-status?scope=all`）。两个必须写进注释的陷阱：①`tagging.ts:58` 的**快速路径**（`ok` + 无向量 + 有文本 → 只 embed 不调视觉）会让重打静默不调视觉，选型 3 正好关掉它；②`running` 行被踩＝同一张图付两次钱，入队要带 `setWhere`，且 `tag_status` 必须用 upsert 的 `returning` 而不是输入 id 列表。**没有迁移**。**诚实项**：评测集跑不了（欠账），这一维准不准**没有数据**；提示词对无害的图也多问了一问，可能抬高整体拒答率（§9.23 无实测支撑）—— 75 张重打是**第一次**能量出这个数。全量重打要花 admin 自己的 `deepseek-flash` 额度、约 **9–20 分钟**（`PER_USER_INFLIGHT=1` 把并发压成 1），**跑之前问一句**。**两端实现已完成**（api `npx vitest run` **399/399**，含新增的 24 条；web 在真浏览器 1264 与真 390 两档验过，`typecheck` + `build` 过），验收各自回填在任务文件里。**只差真库全量重打没跑**——它要花钱，是「`ratings` 从全空变成有值」的唯一验证方式，跑完这一条转 `done` |
| [内容分级独立成第七维 `ratings`（词表 v0.3.0）](2026-09-22-r18-ratings-dimension.md) | `in_progress` | **跨端**——产品负责人 2026-09-22：「现在标签没有色色，加上色色的标签，R18」，查证属实（`色情/擦边/成人/露骨/R18` 在 190 个词条里一个都不沾）。三个选型已拍板：**新增第七维**（不是塞进 `tags`、也不是布尔字段）、**可见性不动**（和别的图一样默认可见——隐藏是读路径决策，捆进来会让一次词表变更变成权限变更）、**一个词条**「成人向」。**两端实现已完成**：api 迁移 0007 对真库跑通、`npx vitest run` **375/375**、那个「`ratings` 列永不写入」的静默漏点在 `tag-queue` 里有哨兵；web 在 1264 与真 390 两档跑过浏览器验收，**`PATCH` body 逐字是 `{"ratings":[]}`**（只发改动那一维）。范围比原计划多一条：用户追问「阅览页怎么看不见 R18」，查证发现**全屏阅览器此前一条元数据都不显示**，于是加了底部信息栏（SPEC §9.24）——`ratings` 排首、描述截断 2 行、`@上传者`；**卡片角标仍然不做**（那是可见性决定）。**三处诚实项**：①这一维的打标主力是**人工**（§9.5 已认定境内模型会拒）；②提示词多问的一问**对无害的图也问**，可能抬高整体拒绝率——**没有实测支撑**；③评测集按 §4.4 必须跑，但前置欠账还在，**没跑**。另有一条**没查清的观察**：编辑面板的 Sheet 在「⋯ → 编辑」这条路上量到 `pointer-events: none`，同一环境下 `Esc` 关菜单与 AlertDialog 都正常，分不清是应用缺陷还是合成事件假象——**没有据此改代码**，详见任务文件 |
| [语义维度拆成五个 + 检索五处修正](2026-09-22-语义维度拆分.md) | `in_progress` | **跨端**——v0.1.0 只有 `emotions` / `scenes` 两维，用户给的例子里**没有一种标法是对的**：微笑角色配「你说得都对」，脸是笑的、心不是开心、语气是敷衍、用途是表面附和，挤在一维里模型只会挑最表面那件（脸）。拆成表情 / 情绪 / 语气 / 用途 / 情境五维（SPEC §9.22），词表 163 → 190、28 个词换维度、迁移搬存量。**同批修掉五个检索缺陷，其中三个不报错**：HyDE 改写**替代**了原查询去编码（丢否定，「不要真人」召回一屏真人）、文本路把 `search_text` 也做 trgm（标签信号被两路各计一次分，SPEC §9.21）、换 embedding 模型期间两代向量混在一起算余弦（SPEC §9.20，过滤时必须同时 `degraded: true`）。两处欠账写在任务文件里：**评测集没跑**（改了词表 + 提示词 + 检索三样）、**web 没在浏览器里开过**；另有 3 个词删除后无 alias 可去（`被催` / `回复长篇大论` / `等待`），跑完迁移找不回来 |
| [导入页（三个页签）迁到 shadcn / Tailwind](2026-09-22-web-import-shadcn.md) | `done` | **web 单端**——`/import` 是 `styling.md` 点名剩下的最后一页（打标页签也在这一页上），整页三个页签全是无层 BEM（`.import__*` / `.review-card*` / `.tagging__*` 约 450 行）。用户原话「优化导入页面样式，按照现在的 shadcn UI 样式和他的组件」，范围选了**整页三个页签**。**这是 `TOUCH` 分档那条规则第一次落到这一页**：原来是无条件的 `min-height: 44px`，鼠标那档本就该收回去（`size="lg"` 落到自己的 40、默认 `size` 落到 36——`min-height` 只抬不压，**不会**掉到 32，两处断言是被实测改写的）。三条「漏了不报错」：①`TabsList` 的 `pointer-coarse:h-auto!` **不能省**——注册表给列表写死 `group-data-horizontal/tabs:h-9`，特异性比工具类高，只有 `!` 压得住，少了它手指那一档的 44 触发器**被 36 的胶囊裁掉**（实测加之前 list 36 / 加之后 52）；②方形的移除按钮用 `size-8 pointer-coarse:size-11` **不能套 `TOUCH`**（`min-h-11` 会把 32×32 拉成 32×44 的长方形）；③卡片左右两栏的堆叠从 `@media (max-width: 640px)` 换成 `@container @max-[560px]:`，**骨架必须同档**否则数据回来时高度跳一下。顺带修一个真缺陷：`.import__item-state--<state>` 这六个类在 CSS 里**根本不存在**，六个状态长得一模一样，现在落成 `STATE_BADGE`——`variant` 是契约不是配色偏好，`needs_review` 仍用中性色、只有 `failed` 是 destructive。另两处：两个长列表（待上传 / 进度）原来会把页面拉成几万像素，改 `ScrollArea` 限高 288 内部滚（虚拟化不做）；打标网格**没照** `DiscoverWall` 的 `WALL_GRID` 抄三档容器查询，改用 `auto-fill`（要的是「列数跟容器走」，`auto-fill` 更直接且没有「`@container` 类被删掉下面几档一起静默失效」那个坑，偏离记在任务文件里）。浏览器 **35/35** 断言过，1600×900（细指针）与 390×844（粗指针）各一遍：页签 32/44、子筛选 32/44、移除按钮 32×32 / **44×44**（方的）、待确认三按钮 36/44、卡片 2 列/1 列、打标网格 6 列/2 列、配额不足真发破坏性 `Alert`（含剩余空间与 requestId）、`/imports/{id}/events` 用真 SSE 端到端跑到 done。构建产物里 `.import__*` / `.tagging__*` / `.review-card*` 选择器 **0 条**。`styles.css` 现在只剩登录页那套（`.auth-page` / `.auth-form__*`，两个路由共用）加一条**全仓零引用**的 `.error`，那是最后一处、留作单独任务 |
| [浏览页迁到 shadcn + 左栏固定 + 顶栏 z-20](2026-09-22-web-browse-shadcn.md) | `done` | **web 单端**——`/browse` 是最后仍是手写 BEM 的业务页，三条已写进规则的契约在这页没人接住：`.browse__clear-btn` 与 163 个 chip 全部低于 44（styling.md 说「没有例外」）、`setError(err as ApiError)` 让断网显示成「加载失败：undefined」、颜色不走 token。**顺带修一个真缺陷**：顶栏 `z-10` 与卡片浮层（`MemeCard` 也是 `z-10`）同层且 DOM 在后，`SidebarInset` 只是 `relative` 不构成层叠上下文 → 滚动时角标画到顶栏上面；裁定顶栏抬到 `z-9999`。**连带代价核实过几何**：Radix 那层 `z-50` 从此在顶栏之下，导航抽屉的品牌行（y=8..56）与编辑面板的关闭按钮（y=16..60）会被顶栏盖住，所以三处 Sheet 一律改从顶栏下面开始（`top-(--app-header-h) bottom-0 h-auto`——**三个都要写**，tailwind-merge 会把注册表的 `inset-y-0` / `h-full` 按冲突组删掉）。另两处「写错了不报错」：断点 640→768 是全站第二个手机断点、桌面筛选列**必须仍是单元素**（套一层内层 `sticky` 会因父元素与自身同高而静默失效）。浏览器 87 条断言全过（桌面 1280 / 手机 390 / 700+768 断点 / 2560 五档），**两处断言被实测改写**：①「抽屉开着时顶栏仍可点」不成立——Radix 的模态给 `body` 挂 `pointer-events: none`，顶栏只是**画在遮罩之上、点不动**（在那一条上按下命中的是遮罩＝关掉抽屉）；②计划里「浏览页因 masonic 单元格带 transform 本来就压不上去」被推翻——单元格是 `position:absolute; transform:none`，`z-index:auto` 不构成层叠上下文，**两页各带一次「改回 z-10 就命中卡片浮层」的对照**，证明浏览页同属这条缺陷。另抓到并修掉一个真缺陷：滚到第二页后换筛选会**拿旧游标发一页**（`nextCursor` 到下次渲染才清，改用同步作废的 `cursorRef`）。2560 下瀑布流由 11 列变 5 列（`max-w-6xl` 封顶，**有意为之**；1280 下逐字相同）。**同日第二批（用户二次验收，推翻上面两处）**：①左栏要「固定、不和右侧一起滚」——量出来 `sticky` 做不到：可粘余量 = 容器高 − 左列高，而左列高恒等于 `max-height`（163 个 chip 永远撑满），容器高 = max(两列)，于是**只要结果列比视口矮余量就是 0**，左列整段跟着页面走（1440×900 / 1920×1080 / 2560×1440 三档实测 68/56/44/32 = 80 − scrollY；1024 / 1280 档结果列够高才钉住）。改成外壳式布局：md 以上容器 `md:h-[calc(100svh_-_var(--app-header-h))]` + `md:-my-6`（抵消外壳 `p-6`）+ `md:py-6`（加回来，两列位置与改前逐像素相同）、两列各 `overflow-y-auto`、去掉 `md:items-start` → **文档不滚，左栏无条件不动**（七档窗口 maxScroll 全 0；结果列滚到底后左列矩形逐字节不变）。列内滚到底照样触发下一页（`IntersectionObserver` 的 root 是视口、祖先裁剪照算）。顺带修掉一个新抖动：竖条占宽 15px 会让 masonic 重排列宽，1440 档稳定在「两条滚动条同时在」的相位（墙 888 > 列 877，右端被切 11px），`[scrollbar-gutter:stable]` 断掉这条回路。②顶栏 `z-9999` → **`z-60`**：只与两个邻居有关（卡片浮层/侧边栏 `z-10`、Radix `z-50`，中间没别的东西），所以与 9999 当前无可观察差别，首轮四条断言全部重测通过；**唯一上限是全屏阅览** `.yarl__portal` 自带 9999，关系从「同值比 DOM 顺序」变成「9999 > 60 比数值」，顶栏那个数不能再加到 9999 以上。③根目录两颗 LOGO 接进侧边栏品牌行（`web/public/` 逐字节副本 + `dark:invert`，`index.html` 加 favicon；已知不足：favicon 没有深色变体）。第二批 65 + 9 条断言全过。**同日第三批（用户三次验收：「两处滚动换成 shadcn 的滚动条组件、分栏换成可拖宽的组件」）**：两列换 `ResizablePanelGroup`（新增 `react-resizable-panels@^4.13.1`，v4 的 API 是 `Group`/`Panel`/`Separator`，装成 v3 整片类型报错）、两处换 `ScrollArea`。四个量过才知道的坑：`Group` 给自己写行内 `height:100%`，断点上改高度只能靠 `!important`（`max-md:block! md:h-[…]!`，少了它容器高=内容高、页面又能滚、左栏跟着跑）；分隔条的 `aria-orientation` 描述的是**分隔条自己**（横向分栏拿到 `vertical`），按「库写组的方向」去改会得到 1281×1 的横线、两个面板 0 宽（注册表文件因此一个字没改）；面板宽度首帧是 0 且 masonic 会把容器 element **重挂一次**（`key={didEverMount}`），一次性首读 + 旧节点上的 observer 两个坑叠加 → 24 张图排成一竖条；窄屏那个 viewport 不滚、没有滚动条要躲，`pr-3` 白扣 12px 正好让列数从 2 掉到 1（(330+12)/172 = 1.98），所以留白写 `md:pr-3`。**同时抓到并修掉一个真缺陷**：`<Masonry>` 的滚动位置来自 `window.scrollY`（第二批之后页面已经不滚），`scrollTop` 恒为 0 → 可见范围永远钉在前 2700px，**滚到下面一片空白**且每追加一页更长（实测滚到列底时最低那张图在视口上方 400px）；改用 masonic 原语自己喂 `scrollTop`/`height`，滚动源按「谁真的在滚」现场判定（手机退回窗口，否则整份渲染）。实测 1600×900：容器 [280,56,1296,844]、分隔条 1px、文档 900/900 无横向溢出；拖 +150 → 筛选 220→370、结果 1051→901、瀑布流 6→5 列；键盘 ← 卡在 `minSize` 168；滚到底渲染 84 格、最低格底 1687（视口底 876）无空白。390×844：块级堆叠、筛选面板高 0、2 列、整页滚（窗口那一路虚拟化）。**同日第四批（用户四次验收：「所有的标签 TAG、按钮都太大了，浏览页筛选板块是最佳实例」）改的是一条规则**：`lib/touch.ts` 的 `TOUCH` 由 `min-h-11` 变成 `min-h-11 pointer-fine:min-h-8`——闸门是**指针**不是宽度（断点会把 700px 桌面窗口当手机、把宽屏平板当桌面；`any-pointer: coarse` 又会让每台触屏笔记本退回 44），一处改动 40 个调用点全跟着降，另三处手写落点（侧边栏 `NAV_ITEM_SIZE`、筛选栏四行、顶栏 ☰）与卡片浮层（`size-8 max-sm:size-11` → `size-8 pointer-coarse:size-11`）一并改。`min-height` 只抬不压，所以细指针那一档是把额外那 12px 收回去、落到各控件自己的 `size` 变体（chip 32、`SelectItem` 36）。实测鼠标/手指两档：chip 32/44、复选框行 32/44、`SelectTrigger` 32/44、下拉选项 36/44、导航项 36/44、顶栏 ☰ 32/44、卡片浮层 32/44，**筛选列内容高 2635 / 3427（−23%）**，首屏 chip 从 11 行变 16 行；手机一个字没变（44 是给手指的）。`/import`、`/tagging` 还没迁，`styles.css` 里那几条无条件 `min-height: 44px` 属它们，迁移时按新规则走。**同日第五批（用户五次验收：「修改筛选顶部那几个的布局（俩筛选都挤到一起了）。多选器换成开关」）**：两个复选框换 `Switch`（**开关在右、字在左**，整行 `justify-between`），四行拆成「开关组 + 下拉组」两组、组内行间 6px——「挤到一起」的成因是 32 高的两个带边框控件上下相接，`gap-4` 的 16px 被边框吃掉。裸 `<label>` 包住 Radix 的 `Switch` **实测**能转发点击并给出无障碍名（AX 树读到「只看动图」「只看收藏」），点文字即翻转 `aria-checked`、URL 与请求都带 `isAnimated=true`（打桩服务端收到、40 张变 8 张）；桌面 32 / 手机 44 两档、缝 6/16/6、chip 163 与列数 6/2 全没动。三个多选 chip 组**有意没换成开关**（它们本来就是多选，换开关会把语义说成开/关）。**同日第六批（用户六次验收：「顶部的 Z-INDEX 调了吗，现在还是和侧边栏展开有冲突，会一起显示」+「导航左侧下面徽标。管理员用 Default 用户用 Outline」）推翻第二批自己的两个决定**：顶栏 `z-60` → **`z-20`**，三处抽屉删掉 `top-(--app-header-h) bottom-0 h-auto` 回到全高（注册表 `inset-y-0` 原样）。`60` 那版是绕路——顶栏高过 Radix 的 `z-50`，抽屉再全高也盖不住它，只能让抽屉**避开**顶栏，于是屏幕上同时出现一条 56px 的顶栏和一个 ☰；而那条顶栏本就点不动（`body` 被挂了 `pointer-events: none`，点它等于点遮罩＝关抽屉），「留着它还能点 ☰」这个理由从头就不成立。`z-20` 仍 > 卡片浮层的 `z-10`，第二批真正要修的那条照旧成立（实测：首页图墙滚到收藏按钮与顶栏相交处，`elementFromPoint` 命中**顶栏**）。三处面板全高后各自的关闭按钮都点得到（筛选抽屉 `[0,0,332,844]`、编辑侧边栏桌面 `[1180,0,420,900]` / 手机 `[0,0,390,844]`、× 中心命中 × 自己）。角色徽标由 `secondary`（两个角色一模一样，扫一眼分不出身份）改成 admin `default` / member `outline`，两档各实测一次 `data-variant` |
| [图片左键全屏阅览](2026-09-21-web-image-viewer.md) | `done` | **web 单端**——四页里的图最大只有 200px（手机 140px），而 `styling.md`「不裁剪是因为信息常在边缘」恰恰说明用户需要看清边缘，当前却**没有任何放大手段**。接 `yet-another-react-lightbox`，`MemeImage` 的帧从 `<div>` 变 `<button>`、点它开全屏。**最要害的一条不是功能本身**：阅览器必须挂在**页面子树之外**——React 的 portal 事件沿 **React 树**冒泡不沿 DOM 树，挂在卡片里的话首页那个挂在根 `<section>` 上的键盘 handler 会连阅览器里的 `Esc` / `↑↓` / `Enter` 一起接走（`Enter` 的表现是**静默发起一次复制 / 下载**，通知的是**另一张卡**）。另外三条漏了都不报错：`<button>` 默认 `inline-block` 会撑出行盒、四页网格与瀑布流一起漂约 7px；Tailwind v4 去掉了按钮的 `cursor: pointer`，不写就还是箭头；`Zoom` 插件的文案在**插件自己那份 labels** 里，只改顶层那两个按钮仍是英文。实测还撞到库的一个行为——单张时它**无条件渲染**左右两个「按了没反应」的箭头（不是 `disabled`、也没藏），而它们盖住的正好是唯一能点到的背景，所以「点背景关闭」几乎点不着；`render` 掉它们是功能的一部分。浏览器 49 条断言连过三次 |
| [首页迁到 shadcn + 拆 `features/search/`](2026-09-21-web-home-shadcn.md) | `done` | **web 单端**——首页是最后一个完全手写 BEM 的业务页：shadcn 控件「默认全部低于 44 且没有例外」这条在这里没人接住，页面也不随主题 token 走。三处行为改动：手机不再自动聚焦（按 `(pointer: fine)` 分流，否则弹键盘盖住随机图墙——这条是[随机图墙](2026-09-19-home-random-grid.md)转出的遗留项）、**「重试」从「清空搜索词把用户丢回图墙」改成真的重跑同一个词**、加 `max-w-6xl`。两处「漏了不报错」的坑：`@max-[900px]:` 生成的是 `@container (width < 900px)`（**`<` 不是 `≤`**，本仓零先例，写错只是永远 5 列）、`Input` 不包 `forwardRef` 则 `inputRef.current` 是 null，**自动聚焦静默不生效**（生产构建连警告都没有）。浏览器 49 条断言全过：5/3/2 三档容器分档、键盘全路径、重试请求计数 +1 且搜索词不变、手机 390×844 不聚焦且没有一枚按钮写「复制」 |
| [卡片 / ⋯菜单 / 编辑侧边栏 shadcn 化](2026-09-21-web-card-shadcn.md) | `done` | **web 单端**——组件库进来了但业务页一个都没用（上一轮顺手删掉了做错的那版 `MemeImage`），四页还是裸 `<img>`；同时 `MemeActions` / `MemeEditPanel` 两块骨架期的手写弹层在解决 Radix 已经做对的焦点进出、点外关闭、层叠。本轮把卡片抽成 `MemeCard` + `MemeImage` 四页共用，菜单换 `DropdownMenu` + `AlertDialog`，侧边栏换 `Sheet`，`styles.css` 少掉三块死规则。**要害是关闭后约 100ms 才发生的那次焦点恢复**——`preventDefault()` 真正兜住的是**点外关闭**那条路（打开确认框那条有模态陷阱兜着，删掉它测试照样过，实测对照写在任务文件里）。**本任务取代已归档的[图片承载组件](2026-09-19-meme-image-host.md)**（那个 `MemeImage` 已被 `f7f6661` 删掉），并取代[浏览页图片操作](2026-09-19-browse-meme-actions.md)验收表里「Tab 能依次走完」那条断言（`role="menu"` 下 Tab 被 Radix 吞掉，是 WAI-ARIA 的规定，**不是回归**） |
| [外壳顶栏吸顶](2026-09-21-web-sticky-header.md) | `done` | **web 单端**——顶栏唯一的常驻内容是折叠按钮，而不吸顶时它只在页面顶部可见，长页面一滚就再也折不动侧边栏。取 `sticky` 不取 `fixed`（后者抽离文档流，还得多补一层 `padding-top`）；代价是三处「必须比它低」的偏移量，收成 `--app-header-h` 一个值：顶栏自己、`.browse__sidebar` 的 sticky `top`／`max-height`、`SettingsCard` 的 `scroll-mt`。三处漏改**都不报错**：筛选栏滑到顶栏底下、锚点落进顶栏里，全靠肉眼。实测对照：`/browse` 滚到 1200 时顶栏 `top` 由 **-1200** 变 **0**，筛选栏由 0 变 56 |
| [迁 Tailwind v4 + radix-luma 组件层](2026-09-21-web-tailwind-v4-radix-luma.md) | `in_progress` | **web 单端**——上一轮只换对了颜色：preset `b1VlIttI` 的 `style` 是 `radix-luma`（不是 `new-york`）、自带 Inter 字体、圆角换算也从加减改成乘法。三层都在，所以「配色像、整体不像」。迁 Tailwind v4 是它的前置；`shadcn/tailwind.css` 是硬依赖（18 个组件靠它定义的 `data-open:` / `data-checked:` 等变体）。代码侧已完成、`/ui` 实测通过，剩业务四页要登录后过一眼才能关单——**侧边导航那次用 route 打桩在浏览器里渲染了 home / browse / import / settings，四页在外壳里都正常**（见[该任务](2026-09-21-web-sidebar-nav.md)的验收表），但那是渲染证据，不是真接口联调 |
| [设置页改用 shadcn 重做](2026-09-21-web-settings-redesign.md) | `done` | **web 单端**——外壳换成 shadcn 之后，`/settings` 里每一块还是 BEM 手写，一套界面两种来源。换皮不是重点：**这页扛着三条不能松的契约**（两段逐字文案、「不截断原始返回」、「不测不让保存」），重做的最大风险是它们在「看起来更现代」的过程中被悄悄削弱，所以验收标准是证明它们活下来了。结构性收益是**触摸目标 44px 从五份手写收成一个常量**（散在 `styles.css` 里各写一遍）。三条实测出来的坑：shadcn 控件默认全部低于 44 且**没有例外**（`size="sm"` 只有 32、`SelectItem` 36）；**Radix 的折叠高度是展开那一刻量一次的快照**（塞 200px 进去，`scrollHeight` 108→292 而 `height` 仍是 108、`overflow: hidden`，只露 16px 且不报错）——这条直接否掉了「高级选项用 Accordion」的方案，因为里面装的正是测试结果；`Accordion` 的 `defaultValue` 只在挂载时生效，两次测试之间组件复用会留着上次展开的原始返回 |
| [顶部导航换成侧边导航](2026-09-21-web-sidebar-nav.md) | `done` | **web 单端**——把 `App.tsx` 那套手写 `header.app__header > nav.app__nav` 换成 shadcn 的 `Sidebar`（桌面折叠成图标窄栏、手机收成抽屉）。两处结构性理由：`.app__nav a { min-height: 44px }` 是触摸目标契约在代码里的**唯一落点**，随顶部导航一起消失，得有人接住；`.discover__grid` 的断点绑在视口上，侧边栏吃掉 256px 后 768–1150px 视口仍排 5 列、每格 120–140px，必须换成容器查询。踩到三个「漏了不报错」的坑（`--sidebar-*` token 缺失、`@theme inline` 不输出变量导致侧边栏圆角全 0、`TooltipProvider` 缺失直接白屏），并修掉一个 registry 缺陷（手机端点导航后抽屉不收起） |
| [浏览页图片操作](2026-09-19-browse-meme-actions.md) | `in_progress` | **跨端**——SPEC §6.4 的 `PATCH` / `DELETE` 写在契约里很久但一行没实现，打标失败的图至今没有出路；卡片「⋯」入口给复制 / 编辑 / 删除，编辑开侧边栏。顺带把首页那段临时的「复制地址」换成 `clipboard-share.md` 的真流程 |
| [浏览页改瀑布流](2026-09-19-browse-masonry.md) | `done` | **web 单端**——浏览页方形网格改瀑布流，按自然宽高比整张展示（不再裁方）；`width/height` 已在响应里、缩略图保比例，故不动 api/SPEC；分页形态已定：保留无限滚动，引 masonry 库（推荐 `masonic`） |
| [图片承载组件](2026-09-19-meme-image-host.md) | `done` | **web 单端**——`MemeCard` 的裸 `<img>` 抽成 `MemeImage`：按比例占位 / 加载骨架 / 失败兜底文件名 / 深色底 / 动图 hover·点按播放，四页共用；关键事实是缩略图本就是静态首帧，「不自动播放」天然满足 |
| [首页随机图墙](2026-09-19-home-random-grid.md) | `done` | **跨端**——首页现在是纯搜索框，没有「翻」的入口；`GET /memes` 加 `random=true` 在全库抽样（SPEC §6.3.2，已转 `accepted`） |
| [打标状态界面](2026-09-19-tagging-status.md) | `in_progress` | **跨端**——打标跑起来了但界面上看不见，兑现 §5.2.3 / `styling.md` / `settings-ux.md §9` 三处已写下的承诺；新增 `GET /memes/tag-status`（SPEC §6.6，**仍 `proposed`**）。**2026-09-22 状态更正**：此前写 `planning` 是错的，两端实现都完成了（api `routes/memes.ts` 的 `/tag-status` + `services/tag-status.ts`，web `TagStatusView.tsx` / `FailureBreakdown.tsx`，各自验收段落都标「完成，待联合验收」）。**卡在联合验收**：本地库里没有 `pending` / `needs_manual` 的图，而「汇总的 `needsManual` = 列表条数」这条正是替身验不了的——要在真链路上造一张 `needs_manual`（或让它连一个不存在的模型地址）走一遍。SPEC §6.6 转 `accepted` 也挂在这一条上 |
| [三个管理页并入设置页](2026-09-18-settings-merge.md) | `in_progress` | **web 单端**——`/admin/*` 三页都是设置项；带出两处 SPEC 措辞偏差待总管裁定 |
| [R2 公开 URL 丢了键前缀](2026-09-18-r2-public-url-prefix.md) | `in_progress` | **api 单端，挡着用**——首次接真实 R2 就全站裂图；派生 URL 有两份实现，都没加 `R2_KEY_PREFIX` |
| [评测集](2026-09-13-eval-set.md) | `in_progress` | 持续优化——边用边跑；**已转入一项工具改造**：`eval.ts` 现在跑的是探测期提示词，不是上线那份。**2026-09-22 新增前置项**：标注还是词表 v0.1.0 的两维口径，30 条里 23 条不合法（27 个换维度的移动，**消失的词 0 个**，是机械迁移不是重标）——不迁就跑，「维度拆对了」会被算成「模型变差了」 |
| [词表 v1](2026-09-13-vocab-v1.md) | `in_progress` | 持续优化——`proposed` 版本直接落代码，跑出数据后迭代 |
| [供应商探测](2026-09-13-provider-spikes.md) | `in_progress` | 持续优化——先选一个能用的，探测结果随用随补 |
| [梗名别名层](2026-09-13-meme-lexicon.md) | `planning` | 检索实现定稿前做完；词表是闭集，梗名是开集，见 [SPEC §9.18](../spec/09-decisions.md) |

**已归档**：骨架、认证、Admin 邀请码与用户管理、浏览页、搜索页、导入、打标队列消费者（含收藏端点）、api 收尾三件、模型配置与测试连接，见 [`_archive/joint-tasks/`](../_archive/joint-tasks/)。

**还没有任务、但已知缺口**：SPEC §6.4 余下的两个接口 **restore / 查重**（编辑与软删已进[浏览页图片操作](2026-09-19-browse-meme-actions.md)；**retag 已开任务**，见上表——在它落地之前 `needs_manual` 的图仍只能人工补标签、不能重跑模型）、`queue.md §6` 的五个定时清理任务、设置页与管理页的统计面板（`settings-ux.md §9`，接口随[打标状态界面](2026-09-19-tagging-status.md)一起做，界面没做）、部署、**`web/` 的常驻 e2e**。

> ~~web 侧 `tagStatus` 徽标直出英文枚举~~ —— 已并入[打标状态界面](2026-09-19-tagging-status.md)（2026-09-19）。

> 最后一条有明确排期：**排在 §6.4 和部署之后**（测试设施不阻塞核心功能）。`web/package.json` 至今只有 `dev` / `build` / `preview` / `typecheck`，没有任何测试框架——已经是第四个用一次性脚本跑几十条断言、跑完即删的任务了，代价是每轮重写驱动、归档里的数字全不可复现。
>
> 沉淀时**不要整包搬**：硬边界（key 不出响应）、错误码路径、403、重建端到端值得留；布局类断言不值得——视觉风格没定稿（`web/AGENTS.md §5`），留下来只会因为装饰改动变红。最该先重建的是那个**六模式的模型上游替身**（`good`/`weak`/`small`/`badkey`/`offvocab`/`refuse`），768 维、上游 401、词表越界这些路径拿真供应商凑不出来。设计记在[模型配置任务](../_archive/joint-tasks/2026-09-16-ai-config.md)的 web 验收里。

> ✅ **两端 typecheck 口径已对齐**（2026-09-16，见[api 收尾三件](../_archive/joint-tasks/2026-09-16-api-housekeeping.md)）。`api/tsconfig.json` 现在也开着 `noUnusedLocals` + `noUnusedParameters`，所以本端自查和提交前闸门看到的是同一批错误。
>
> 这条留在这里是因为**那次的漏法还会再来**：`web` 的类型链会把 `api/src/` 一起编译（`api/package.json` 的 `exports` 指向 `./src/app.ts`），任何一端往 `tsconfig.json` 加严格开关而另一端不加，就又会出现「api 自查全绿、闸门红、报错全在 api 代码里」。闸门始终是 `cd web && npm run typecheck`，见[骨架任务](../_archive/joint-tasks/2026-09-13-skeleton.md)第 10 条。

## 模型配置任务转出的遗留项

出处见[该任务归档](../_archive/joint-tasks/2026-09-16-ai-config.md)的第四轮联合验收：

| 遗留项 | 归属 |
|---|---|
| 「全站重建索引已排队 N 条」那句话**没有人在浏览器里见过**——代码路径跑过，但 `${n}` 前后的空格、窄屏会不会挤断行只有渲染出来才知道 | **部分销账**（2026-09-22）：验的是**同卡另一句**——手动按钮的反馈也带 `${n}`，在真 Chrome 里过了 1264 / 390 两档。`EmbedSettings` 那句走的 `PUT /config/embed` 回执，需要「填表 → 测试连接 → 保存 → 确认框」整条流程，那一档**仍未在浏览器里见过**，留着 |
| `POST /admin/reindex` 的 `enqueuedCount` **没有任何界面消费它**（`startReindex()` 按契约只看 `res.ok`，理由正当），只被测试和日志消费 | ✅ **已消费**（2026-09-22）：`ReindexPanel` 拿它区分「已排队 N 条」与「没有新排队的记录」。原判不是缺陷，但它掩盖了一个**静默**——`enqueuedCount` 为 0 时界面与点击前逐像素相同（徽标还是「空闲」、进度还是 100%、三个计数不动），用户只能反复点。补了 `api-contract.ts` 的编译期断言：字段改名会让 `npm run typecheck` 炸，否则前端只会永远走 else 那一支、永远说「没排上队」，而实际排了 |
| 模型上游全程是替身。真供应商的超时、限流、流式截断、各家 `error` 体型一条都没验过 | [供应商探测](2026-09-13-provider-spikes.md) |
| 设置页与管理页只在 Playwright 的 390×844 视口测过，**不是真机**。本任务两页不涉及复制 / 分享路径所以够用 | 动复制 / 分享路径时必须上真机（`web/AGENTS.md §6`） |

## 打标队列任务转出的遗留项

出处见[打标队列任务归档](../_archive/joint-tasks/2026-09-16-tag-queue.md)的总管裁定：

| 遗留项 | 归属 |
|---|---|
| 评测集未跑；**`eval.ts` 内嵌探测期提示词，不 import `src/ai/vision.ts`**，原样跑出的数字会被误读成「新提示词已验证」 | [评测集](2026-09-13-eval-set.md)，已进其「做完的标准」 |
| `tag_status = 'refused'` 目前不可达（只有主通道，终局失败全落 `needs_manual`） | 等 SPEC §9.5 转 `accepted` + 副通道接入；**不是缺陷**，不要为了让状态可达提前实现 §9.5 |
| 设置页会在 `VISION_NOTICE` 底下多一行「副通道的配置入口尚未开放」——因为契约文案让用户去配副通道，而界面上没有那个输入框（2026-09-18 总管裁定，见[模型配置与测试连接](../_archive/joint-tasks/2026-09-16-ai-config.md)的联合验收） | **同样绑 §9.5**：副通道入口一开放，这行字必须删。它是对界面现状的说明，不是契约的一部分，所以写在 `<pre>` 契约块之外——契约文案仍然逐字可比对 |
| 降帧梯子 `[10, 4, 1]` 生产里走不到：`resolveVisionConfig()` 固定 `multiImage: null` | ✅ api 端已修（2026-09-18，`81e1aeb`）：能力位跟着配置来源走，用户测出 `multiImage: true` 后梯子才是真实流量路径。部署方默认通道仍固定 null，那是有意的 |
| `finish_reason: 'length'` 归 `AI_INVALID_OUTPUT` 的判断只活在任务文件里 | ✅ 已完成，进了 `api/agents/rules/ai-providers.md §3`（见[api 收尾三件](../_archive/joint-tasks/2026-09-16-api-housekeeping.md)） |

## api 收尾三件转出的遗留项

出处见[该任务归档](../_archive/joint-tasks/2026-09-16-api-housekeeping.md)的总管裁定：

| 遗留项 | 归属 |
|---|---|
| `routes/auth.ts` 的 `/me` 自己重复了一遍会话解析，与 `middleware/auth.ts` 的 `resolveUser` 同一套逻辑 | api 本端重构，非紧急；改时确认错误码仍是 `UNAUTHENTICATED`（SPEC §6.1：401 即跳登录页） |
| `finish_reason: 'length'` 的真实频次**没有实测支撑**——规则里已明写这一点，别把它当已验证的结论引用 | [供应商探测](2026-09-13-provider-spikes.md)，跑出来再补 |

## 导入任务转出的遗留项

出处见[导入任务归档](../_archive/joint-tasks/2026-09-15-import.md)的联合验收。前三项共用一个前置条件——**部署 + 库里有真数据**，不需要两端再写代码：

| 遗留项 | 归属 |
|---|---|
| 真 api × 真浏览器联调未跑（web 断言全部对着替身）；重点看 `existing` 为 null 的降级、`sizeBytes` 字符串、SSE 断线→快照→重连 | 部署后的联合回归 |
| `QUOTA_EXCEEDED` 的 `remaining` 展示与「停止后续上传」、`INTERNAL` 的 `requestId` 展示只走了代码路径 | 同上，需真实配额环境 |
| 导入路径在移动端 / 触屏、Safari、Firefox 未测 | 同上 |
| 剪贴板 `Ctrl+V` 入口、拖拽文件夹递归（`webkitGetAsEntry`）未实测 | web 本端，补测时机自定 |

## 搜索任务转出的遗留项

这些不阻塞任何新任务，各自独立跟踪（出处见[搜索任务归档](../_archive/joint-tasks/2026-09-15-search.md)的联合验收）：

| 遗留项 | 归属 |
|---|---|
| 未跑评测集（`recall@5`）；本次动了 RRF 参数、HyDE 提示词、切词 | [评测集](2026-09-13-eval-set.md) |
| HyDE 走部署方 `env.defaultVision`，未按搜索者自己的视觉通道解析——**与 SPEC §6.3.1 的已知偏差** | ✅ api 端已修（2026-09-18，`81e1aeb`）：`rewriteQuery` 收 `actorId`，登录用户走本人通道，匿名落部署方默认 |
| `resolveEmbedConfig()` 只读环境变量，`embed_config` 表无读写路径 | ✅ api 端已修（2026-09-18，`81e1aeb`）：配置表优先、环境变量兜底，只认 `verified_at` 非空的行 |
| `rrf.ts` 依赖「`data/` 每路返回的 id 不重复」，上游 join 出重复行会静默翻倍 | 改 `api/src/data/search.ts` 的 join 时回看 |


## 首页随机图墙转出的遗留项

出处见[该任务](2026-09-19-home-random-grid.md)的两端验收。

| 遗留项 | 归属 |
|---|---|
| **手机端首页会自动聚焦搜索框 → 弹键盘 → 盖住刚做好的随机图墙。** `routes/home.tsx` 的 `autoFocus` 是本任务之前就有的（为了「打开就能打字搜」），图墙出现之前它没坏处；现在它对手机——也就是 `styling.md` 说的「这个产品体验最好的一端」——把整块新内容挡住了。**改它要动搜索路径的行为，而弹不弹键盘、盖住多少只有真机能回答**，所以没在这次版式改动里顺手改 | web 本端，**需要真机**。下次上真机时先看一眼这个 |
| 图墙的空库态与请求失败态**没有真实场景验证**（本地库有图、api 正常），走的是代码路径 | 部署后联合回归，与「真 api × 真浏览器联调」那批一起 |
| 随机卡片上**没有复制 / 发送入口**，只有收藏。有意留的：复制现在是一段临时实现，真流程（`clipboard-share.md`）要求桌面 + 真机各测一遍 | `clipboard-share.md` 的三条路径落地那次，两处一起接 |
| `order by random()` 是全表扫描。几万行毫秒级，**百万行会是秒级**。触发条件已写进 `data/memes.ts` 的注释 | 到量了再换，不提前优化。改 `listMemes` 时回看 |


## 写任务的要求

每个任务文件至少要有：**为什么要做**、**做完的标准是什么**、**两端各自要改什么**。

不要写成一个待办清单。三个月后回来看时，「当时为什么决定这么做」比「做了哪几步」重要得多。

决策结论写完后要搬进 [SPEC §9](../spec/09-decisions.md)——**任务是过程，SPEC 是结论**。
