# 评测集

**状态：`in_progress`** · 创建 2026-09-13

## 为什么

这是本项目投入产出比最高的一件事。没有它，下面这些问题只能靠拍脑袋：

- 打标提示词改了，是变好还是变坏？
- 用户想换一个模型，那个模型够不够格？
- 词表这么设计，不同模型真的能对齐吗？
- 0.6B / 4B / 8B 三档 embedding，差距值不值那点延迟？

在模型可配置 + 库共享之后，第 2 条尤其重要：**用户配的模型会影响所有人**，而「这个模型够不够格」需要一个客观答案。

完整论证见 [`docs/eval.md`](../docs/eval.md) 与 [SPEC §9.12](../spec/09-decisions.md)。

## 做完的标准

- [x] 30 张图选定，覆盖面按 [`docs/eval.md` §2](../docs/eval.md) 的表 · 2026-09-13
- [x] 每张图标注 `expect`、`queries`、`humanName` —— **AI 起草版**
- [~] **人工逐条校对标注** —— 已完成 3 条（`395` `332` `698`），`status: draft-partially-reviewed`。剩余见下
- [x] 补完 `humanName` · 2026-09-13 库主人逐条填写，**14/30 有名字，16/30 本人也认不出**
- [ ] 校对 `queries` 与其余 27 条的 `emotions` / `scenes`，`status` 转 `reviewed` · 流程见 [`docs/fixtures/eval/README.md`](../docs/fixtures/eval/README.md)
- [x] `manifest.json` 落在 `docs/fixtures/eval/`
- [ ] 30 张图本体归集到私有存储，`docs/fixtures/eval/images/` 不进仓库
- [ ] `api/scripts/eval.ts` 能跑通并输出五个指标，**且实现 `acceptAlso` 语义**（漏了会把 `698` 算成假污染）**和名字 query 的多目标召回**（`塞西莉亚` 对应 3 张图，按单目标写会算错）
- [ ] 至少跑过 2 个不同视觉供应商，结果记在本文件末尾
- [ ] 词表合规率的结果反馈给[词表任务](2026-09-13-vocab-v1.md)
- [ ] **`tags.subject` 区分度的结论**反馈给[词表任务](2026-09-13-vocab-v1.md)——见下

> ⚠️ **未校对之前不要跑「标签命中率」。** 拿 AI 起草的期望值去打分，量的是「模型和起草它的 AI 像不像」，不是质量。
>
> **但这条闸门只管命中率那一项，另外三样现在就能跑，而且结果是真的：**
>
> | 现在能跑 | 为什么不依赖校对 |
> |---|---|
> | **词表合规率** | 只看输出是否 ⊆ `vocab.json`，压根不用比对期望值 |
> | **`humanName` 污染率 / 召回基线** | `humanName` 已经由库主人填定（14/30），这一栏是终稿 |
> | **拒绝形态、多图上限、JSON 有效性** | 纯行为探测，没有「正确答案」这个概念 |
>
> 所以[供应商探测](2026-09-13-provider-spikes.md)不必等校对，它和这三样是同一件事、同一个脚本。

> ⚠️ **`recall@5` 跑不了，直到[骨架](2026-09-13-skeleton.md)和检索实现完成。**
>
> 任务索引把评测集列为「写核心代码之前必须先做」，但 `recall@5` 需要 embedding + pgvector + 三路混合，也就是需要它所阻塞的那些东西。**这两件事在文档里是互相依赖的，现实里不是。**
>
> 实际的次序是：`eval.ts` 拆成两半——**打标那半（四个指标里的三个 + 命中率）现在就能独立写**，只要一个供应商和这 30 张图，不碰数据库；**检索那半（`recall@5`）排在骨架之后**。不要等一个能一次跑完五个指标的脚本，那个脚本要到很后面才存在。

## 第一批校对的结果（2026-09-13）

挑了四个起草时最没把握的判断去问库主人。**四个里错了一个**：

| | 起草版 | 人工裁决 |
|---|---|---|
| `395`「互上扣床来！」 | 暴躁 / 挑衅 · 催促 · query「快出来」 | **全错。** 是打乱字序的性暗示，正序才是原意 |
| `332`「我应该是失宠了」 | 委屈 / 阴阳怪气 / 假笑 | 对，不改 |
| `698` 抿嘴微笑配爱心 | 假笑 / 得逞 / 满足 | 两种读法都成立 → 新增 `acceptAlso` 字段 |
| `humanName` 能填多满 | 推测「会很满」 | **确认大部分角色叫得出名字** |

这条记下来不是为了记账：**错的那条读起来毫无破绽**。「AI 起草、人扫一眼点头」这个流程如果真的走了，`395` 会带着一个完全相反的期望值进基准，而且没人会发现。

## 第二批：`humanName` 全部由库主人填写（2026-09-13）

30 张逐条填完，**14 张有名字、29 条去重别名、16 张本人也认不出**。三处起草版被直接推翻：

| | 起草版 | 库主人填的 |
|---|---|---|
| `830` | `fw` | **删掉。** `fw` 是图上的字、已在 `ocrText` 里，文本通路本来就搜得到 —— 这一栏记的是作品/角色身份，不是梗短语 |
| `480` | `猫娘` | `猫娘` / **`猫羽雫`**，补上了真正的角色名 |
| `768` | 无名，当成一张无关的 60px 简笔画 | **塞西莉亚**，和 `161` / `723` 同一个角色 |

`768` 那条最值钱：`塞西莉亚` 这一条 query 有 **3 个正确答案**，横跨带水印漫画、干净漫画、60px 简笔画三种画质。全集最狠的召回测试，而起草时完全没看出来这三张有关系。

顺带证伪一条计划：**`original_filename` 入 trgm 这条路在 QQ 来源上是死的** —— 导出的 `pack_info.json` 里 989 张全叫 `表情_N`，人工命名过的 0 条。已同步给[别名层任务](2026-09-13-meme-lexicon.md)。

## 选图结果

30 张选自 989 张 QQ 收藏库，清单与理由在 `manifest.json` 的 `cover` / `notes` 字段里。几张值得单独知道的：

| 图 | 为什么它不可替代 |
|---|---|
| `139` GIF | 头顶电池图标 满→空→半→满，人物几乎不动。**首帧和末帧都是满电**，只取首帧会得到相反语义；变化的是图标不是文字，OCR 也救不了。**这是抽帧方案唯一无法被其他通路替代的证据**，见 [SPEC §9.4](../spec/09-decisions.md) |
| `944` / `949` / `951` | 同一张底图配三条不同字幕，一条干净、两条擦边。画面完全相同 → **是否被拒只可能由文字触发**。这是 [§9.15](../spec/09-decisions.md) 拒绝形态探测唯一干净的样本，别浪费 |
| `161` / `723` | 同一张漫画，一张干净、一张被平铺水印盖满，dHash 距离 **1**。它们**该**判为重复——问题是保留哪张，因为水印那张的 `ocrText` 会被污染。去重保留策略需要依据，这对就是 |
| `958` GIF | 120 原始帧 → 去重 50，全集最长，采样必然触发；同时是擦边动图，**抽帧后 10 张里任一张被拒整条就失败**，降级要在多图场景下也成立 |
| `118` | 扩展名 gif 但只有一帧，必须判为静图 |
| `395` | 打乱字序的性暗示大字。`ocrText` 存乱序、用户搜正序 → **pg_trgm 那一路必然落空**，三路混合在这张上只剩两路。同时是拒绝形态的第二根轴：测模型解不解得开乱序 |

选图没有靠肉眼找相似——**全库 pHash 两两比对后按 Hamming 升序看**，989 张里 ≤8 的有 40 对，上面几组都是这么翻出来的，人工翻不到。

## 两端各自做什么

| 端 | 做什么 |
|---|---|
| 总管 | 选图、标注、维护 `manifest.json` |
| api | 写 `scripts/eval.ts`，输出指标 |
| web | **不参与**，这个任务不碰前端 |

严格说这不是跨端任务，放这里是因为**它阻塞的东西横跨两端**（词表转 `accepted` 之前，`web` 的筛选 UI 不该对具体词条做假设）。

## 标注由谁来定

草稿可以由 AI 起草，但**基准值必须由人逐条改过**。

「期望搜到这张图的 query 是什么」「这张图算不算阴阳怪气」没有客观答案，只有**这个库的实际使用者**的答案。如果标注是 AI 写、人扫一眼点头，那评测集衡量的就是 AI 的先验和 AI 自己是否一致——分数会很好看，且毫无意义。

**扫一眼就通过 = 没有基准。** 至少要真改掉一部分，才说明看过。

## 注意

> ⚠️ **评测图片可能含擦边内容**（覆盖面里有这一类）。如果仓库会公开，图片本身必须放在仓库之外，`docs/fixtures/eval/` 只留清单和标注。

[`docs/eval.md` §2.3](../docs/eval.md) 的七个坑**最容易被忽略**，尤其是「同角色不同表情」和「大留白小主体」。它们测的不是"能不能查出重复"，是"**会不会把不该判为重复的判成重复**"——那才是 [SPEC §9.7](../spec/09-decisions.md) 把判断权交给人的原因。

> ⚠️ **选图前先看 [`docs/eval.md` §2.1](../docs/eval.md)。**
>
> 首个真实库是**二次元贴纸库**，不是中文梗图库：没有截图对话梗、没有明星鬼畜、约八成无文字、83% 近正方形、13% 长边不足 256。照着「表情包」的刻板印象选图，选出来的评测集测不到这个库真正会出问题的地方。
>
> 但**「同模板换字」是有的**（`944` / `949` / `951`）——这一条最初写成了「没有」，是 pHash 全库比对翻出来推翻的。凭印象断言库里「没有什么」，比断言「有什么」更容易错。

## 这个任务会顺带暴露的四件事

发现于 2026-09-13 翻库、起草标注与第一批校对时，**不在本任务的验收标准里，但必须记下来别丢**：

1. **`tags.subject` 已经实测失效，不是推测。** 选出的 30 张里 `subject` 全部是「动漫角色」——**30/30**，`style` 是「动漫」的 25/30。标签过滤那一路在这个库上没有区分度，三路混合实际退化成两路，这动摇的是 [SPEC §9.10](../spec/09-decisions.md) 的前提。交给[词表任务](2026-09-13-vocab-v1.md)决定 `subject` 是否重新设计。
   > 但 `无文字` / `带字幕` 这一对有信号（15 / 9），它直接指明该走哪条检索通路。精简词表时不要连它一起砍。
2. **[§9.18](../spec/09-decisions.md) 的别名层是主路径，但收益有个明确天花板。** 库主人填完 `humanName`：**14/30 有名字（29 条去重别名），16/30 本人也叫不出**。有名字的那些（`塞西莉亚` `kipfel` `井芹仁菜` `樱羽艾玛`）视觉模型一个都说不出来，别名层确实是唯一通路；但认不出的那 16 张，别名层再完善也补不了，只能靠 `description` 向量。**收益按 47% 的图算，不是全库。** 数据和三条实现结论已交给[梗名别名层任务](2026-09-13-meme-lexicon.md)，其中「通用词劫持」（`本` `教授` `BA` 都被填成了别名）是比原计划的「耄耋」问题严重得多的风险。
3. **`emotions` 和 `scenes` 的边界不自明。** 起草这 30 条标注时，对着词表仍然把 `讨好` `看戏` `撒娇` `心虚` 填进了 `scenes` **共 10 处**，靠脚本校验才抓出来。起草者对着词表都会错，模型只会错得更多——这不是手滑，是词表设计问题。交给[词表任务](2026-09-13-vocab-v1.md)。
4. **文本通路会在真实输入上整条失效，`395` 是第一个实证。** 图上写的是打乱字序的句子，`ocrText` 只能照抄乱序（替用户「修正」成正序就是在编造图上没有的字），而用户搜的是正序——pg_trgm 匹配不上，[§9.10](../spec/09-decisions.md) 的三路在这张上只剩两路。同一张图还证明了：模型把乱序**强行解释成一个通顺但错误的意思**是最难发现的污染形态，因为结果读起来完全正常（起草者就是这么错的）。交给 api 端写打标提示词时明确：宁可照抄不解释。

## 结果记录

（跑完后在这里追加，每次一段：日期、模型、五个指标、结论）
